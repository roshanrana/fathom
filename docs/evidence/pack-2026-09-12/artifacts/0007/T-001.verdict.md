# Verdict T-001 — PASS
Reviewed: 20ff9b377a57cbc99388b4bc79d6b1ddd8c5888e  Tier: T2  Attempt: 1

## Acceptance criteria
- AC1: PASS — `quote_card("AAPL", data_dir)` gives as_of=2026-09-11, last_close=332.27, prev_close=326.57, change_pct=1.75, week52_high=344.27, week52_low=228.18 — identical to `data/SOURCES.md` §Anchors. Independently recomputed from `data/bars.parquet` with pandas (sorted, filtered AAPL, 424 bars) and got the same six values. `test_fr002_aapl_anchors_match_sources` hard-codes these as module-level constants copied from SOURCES.md.
- AC2: PASS — `test_fr002_series_is_trailing_ascending_and_matches_last_close` asserts `len(series) <= 252`, ascending dates, `series[-1].close == last_close`; code builds `series` from `frame.tail(252)` on the ascending-sorted frame.
- AC3: PASS — independently recomputed JPM's ytd_pct/one_year_pct/thirty_day_pct straight from `bars.parquet` (bypassing `fathom.quotes`): ytd_pct=12.12, one_year_pct=18.81, thirty_day_pct=-2.45 (base closes 317.7085.../299.8241.../365.18 per the on-or-before rule). `quote_card("JPM", data_dir)` returns the same three values. The test file's own `_independent_bars`/`_closest_on_or_before` helpers do an equivalent independent recompute per definitions in §2.4.
- AC4: PASS — `test_fr002_snapshot_fields_come_from_quotes_parquet` checks `snapshot_source == QUOTES_SOURCE`, `snapshot_as_of.tzinfo is not None` and `utcoffset() == timedelta(0)` (UTC). `test_fr002_snapshot_nan_field_becomes_none` builds a synthetic quotes frame with `market_cap = NaN` and asserts the field surfaces as `None` while sibling fields stay populated; `_optional_float` uses `pd.isna`.
- AC5: PASS — `test_fr002_unknown_ticker_raises_unknown_ticker` asserts `FathomError.code == Code.UNKNOWN_TICKER` for ticker "ZZZZ", raised via `data.require_ticker`. `test_fr002_does_not_mutate_cached_frames` snapshots deep copies of the cached bars/quotes frames before calling `quote_card` for AAPL and JPM and asserts equality after — `_bars_for` does `.copy().sort_values(...)`, i.e. copy-before-sort, never mutating the `functools.cache`d frame from `load_frame`.
- AC6: PASS — `uv run mypy fathom` → "Success: no issues found" (strict config from pyproject, verified clean). Test names: `test_fr002_aapl_anchors_match_sources`, `test_fr002_series_is_trailing_ascending_and_matches_last_close`, `test_fr002_snapshot_fields_come_from_quotes_parquet`, `test_fr002_snapshot_nan_field_becomes_none`, `test_fr002_unknown_ticker_raises_unknown_ticker`, `test_fr002_does_not_mutate_cached_frames`, `test_fr003_jpm_ytd_pct_matches_independent_recompute`, `test_fr003_jpm_one_year_pct_matches_independent_recompute`, `test_fr003_jpm_thirty_day_pct_matches_independent_recompute` — all `test_fr002_*`/`test_fr003_*` per rtm: [FR-002, FR-003].

## Verification checklist
- [x] scope respected — `git show --stat HEAD` touches only `fathom/quotes.py` (new), `tests/test_quotes.py` (new), and `docs/tasks/T-001-quote-card.md` (Handoff section, +7 lines) — matches pack Scope plus the allowed pack/Handoff edit.
- [x] error taxonomy used — `FathomError`/`Code.UNKNOWN_TICKER` (via `require_ticker`) and `Code.DATA_MISSING` (empty bars) from `fathom/errors.py`; no bare exceptions.
- [x] no sensitive fields logged — no logging/print statements in `fathom/quotes.py`.
- [x] tests named with RTM IDs — all 9 tests are `test_fr002_*` or `test_fr003_*`.
- [x] no new dependencies — only `pandas`/`pydantic` used, both already dependencies; `pyproject.toml` untouched in this diff.
- [x] anchors in the test match `data/SOURCES.md` — `_AAPL_AS_OF/_AAPL_LAST_CLOSE/_AAPL_PREV_CLOSE/_AAPL_CHANGE_PCT/_AAPL_WEEK52_HIGH/_AAPL_WEEK52_LOW` are byte-identical to the AAPL row in §Anchors.

## Findings
None.

## Gate
`uv run python scripts/check.py` → all checks passed (ruff check, ruff format --check, mypy fathom [+ --platform linux], secrets scan, pytest); 34 passed, coverage 98.40% (fathom/quotes.py 98%, required ≥80%).
