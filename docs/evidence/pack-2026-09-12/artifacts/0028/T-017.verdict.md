# Verdict T-017 — PASS
Reviewed: 3c7f737543fbd872e1c774a794d63d852a250b37  Tier: T2  Attempt: 1

## Acceptance criteria
- AC1 (AppTest: six metrics, Last-close delta `^[+-]\d+\.\d{2}%$`): PASS. Independent
  AppTest (own script, offline provider, `FATHOM_AUDIT_PATH` in a fresh temp dir) shows
  6 metrics in order Last close / 52-wk low / 52-wk high / Market cap / P/E / Dividend
  yield; `at.metric[0].delta == "+1.75%"`, matching the regex, and no metric value
  contains "…".
- AC2 (`docs/assets/01-header-quote.png` regenerated, no ellipsis, two rows of three):
  PASS. Read the PNG directly — row 1: Last close 332.27 (delta badge "+1.75%" only, no
  absolute-change text), 52-wk low 228.18, 52-wk high 344.27; row 2: Market cap
  "$4.85 T", P/E 38.10, Dividend yield 0.32%; one source caption ("Prices: … · Snapshot:
  …") beneath row 2. No truncated/ellipsized value anywhere. All `docs/assets/*.png`
  checked ≤ 600 KB (44,095 / 144,433 / 77,893 / 425,928 / 70,372 bytes).
- AC3 (full gate green; test names `test_fr012_*`): PASS. `uv run pytest
  tests/test_app.py -q` → 10 passed. `uv run python scripts/check.py` → ruff, ruff
  format, mypy (x2), pytest (287 passed, 96.02% coverage), secrets scan, bench, bench
  drift, card drift — "all checks passed". `test_fr012_ac1_*` present and asserts the
  delta regex (line 45 of tests/test_app.py).

## Findings
Diff is scoped exactly to app/components/rendering.py, tests/test_app.py,
docs/assets/{00,01,02}-*.png, and the task pack (handoff notes) — matches T-017's
declared scope; app/main.py untouched, as the pack allowed. `render_quote_metrics` now
renders two `st.columns(3)` blocks (row1/row2) per D-011's amendment; no new
computation was introduced — `format_market_cap` predates this task (T-016/D-011) and
`test_fr012_ac6_no_computation_leaks_into_app` (banned terms: read_parquet, BM25,
re.compile) passed in the full gate. No issues found.

## Gate
`uv sync --all-extras` clean. `uv run pytest tests/test_app.py -q`: 10 passed.
`uv run python scripts/check.py`: all checks passed (287 passed, 96.02% coverage).
