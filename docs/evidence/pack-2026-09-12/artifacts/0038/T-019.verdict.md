# Verdict T-019 — PASS
Reviewed: 2ea30ba28d13b49291fc21a105fdac2fb9ec1f91  Tier: T2  Attempt: 1

## Acceptance criteria

- AC1 — PASS. Own MockTransport: Yahoo fixture → 30 rows, columns `[symbol,date,open,high,low,close,volume]`, all five OHLCV columns `float64`, `date` are `datetime.date`, ascending, `symbol=="AAPL"`, `last_source=="yahoo"`. BRK.B: combined single call (yahoo fails 500 → stooq) produced requests `.../chart/BRK-B?range=2y&interval=1d` then `.../q/d/l/?s=brk-b.us&i=d` — both symbol mappings verified in one fallback path.
- AC2 — PASS. Yahoo 500 and Yahoo 200-with-`{"chart":{"error":...}}` (no `chart.result`) both fall back to Stooq, `last_source=="stooq"`, 30 rows. Stooq 503 and Stooq 200-with-maintenance-HTML both raise `SOURCE_HTTP`, `details["source"]=="stooq"`, and injected secret strings (`"maintenance detail"`, `"SECRET-INTERNAL-DETAIL-1234"`) never appear in `details` or `message` — confirms `errors.py`'s "never a body" contract holds through `prices.py`. `price_source="stooq"` reverses order (own script + repo test both confirm; repo test also shows Stooq-primary-then-Yahoo-fallback path).
- AC3 — PASS. `test_nfr013_daily_bars_cached_second_call_zero_transport_calls`: second call makes 0 transport calls, same `close` values as first.
- AC4 — PASS, independently hand-recomputed from the four raw fixture files (not from the test's inline comments): shares = latest by `end` regardless of frame → 2026-07-17, 14,594,180,000; EPS TTM = entries matching `^CY\d{4}Q\d$` (excludes the un-framed duplicates and the `CY2025` annual entry) → 1.57+2.84+2.01+2.02 = 8.44; equity = latest `^CY\d{4}Q\dI$` → 2026-06-27, 107,520,000,000; DPS TTM = matching quarterly frames → 0.26+0.26+0.26+0.27 = 1.05. market_cap = 332.27 × 14,594,180,000 = 4,849,208,188,600.00; pe = round(332.27/8.44,2) = 39.37; pb = round(4,849,208,188,600/107,520,000,000,2) = 45.10; dividend_yield = round(1.05/332.27×100,2) = 0.32. All four match the implementation's output exactly via an independent script (own MockTransport, no reuse of the pack's test module). `snapshot_source` matches the Handoff's declared fixed string.
- AC5 — PASS. 404 on shares → `market_cap`/`pb` None, `pe`/`dividend_yield` still computed (verified independently). Repo's fuzz test (empty `units`, missing `frame`, non-numeric `val`, no `units` key at all) raises no exception and yields the expected None/0.0 fields.
- AC6 — PASS. `mypy fathom` (strict=true in pyproject.toml) clean. Full gate `uv run python scripts/check.py`: ruff check/format, mypy (both native and `--platform linux`), pytest, secrets scan, bench/bench-drift/card-drift all green — "all checks passed". 339 passed, coverage 95.88% (`fathom/live` submodules 94–97%, well above the 85% target in §6). Tests named `test_fr022_*`, `test_fr023_*`, `test_nfr013_*`. `HTTPS_PROXY=127.0.0.1:9` in effect throughout — no real network reachable.

## Hand recomputation (inputs → expected → actual)

| Field | Inputs (from raw fixtures) | Expected | Actual |
|---|---|---|---|
| shares | latest end 2026-07-17 | 14,594,180,000 | 14,594,180,000 |
| eps_ttm | CY2025Q2 1.57 + CY2025Q4 2.84 + CY2026Q1 2.01 + CY2026Q2 2.02 | 8.44 | 8.44 |
| equity | latest CY####Q#I, end 2026-06-27 | 107,520,000,000 | 107,520,000,000 |
| dps_ttm | CY2025Q2 0.26 + CY2025Q4 0.26 + CY2026Q1 0.26 + CY2026Q2 0.27 | 1.05 | 1.05 |
| market_cap | 332.27 × 14,594,180,000 | 4,849,208,188,600.00 | 4849208188600.0 |
| pe | 332.27 / 8.44 | 39.37 | 39.37 |
| pb | 4,849,208,188,600 / 107,520,000,000 | 45.10 | 45.1 |
| dividend_yield | 1.05 / 332.27 × 100 | 0.32 | 0.32 |

## Findings

- Both declared Handoff deviations hold as accepted: (1) Stooq maintenance fixture is a JS bot-challenge page, and `_stooq()` treats any body not starting with the literal CSV header `"Date,Open,High,Low,Close,Volume"` as failure — independently confirmed with a fresh maintenance-HTML mock, so the "any non-CSV body is failure" condition for accepting deviation 1 is satisfied. (2) `snapshot_source` is the fixed string with no price-source suffix, per Planner's stated acceptance (deferred to T-020's materializer).
- No scope violations: only files listed in the pack's Scope were touched (`git show HEAD --name-only`); `fathom/live/http.py` and `fathom/live/sec.py` (T-018, frozen) untouched; no `pyproject.toml`/`uv.lock` changes (no new dependencies).
- `SOURCE_HTTP` errors never carry response bodies anywhere in the reachable paths (`http.py`, `prices.py`); verified by injecting distinctive secret strings into mocked failure bodies and asserting absence from both `details` and `message`.
- Minor, non-blocking observation: DPS-TTM logic treats "concept present but zero matching quarterly frames" the same as "concept absent" (both → 0.0), which is a slightly broader default than the spec's literal "0 if the concept is absent" — not exercised by AC5/tests, does not contradict any acceptance criterion, LOW severity.

## Gate

All commands green: `uv sync --all-extras`; targeted pytest (18 passed); `mypy fathom` (strict, clean); full `scripts/check.py` gate (339 passed, 95.88% coverage, ruff/mypy/secrets/bench all green). PASS.
