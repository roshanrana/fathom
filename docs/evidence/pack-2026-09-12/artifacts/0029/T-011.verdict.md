# Verdict T-011 — FAIL

Reviewed: 9eff1ce (docs) at head 1dde70d59b880a5dffb08fecbb911518de262ba0  Tier: T2  Attempt: 1

## Acceptance criteria

- **AC1 — README commands run as written: PASS.**
  `uv sync --all-extras` succeeded. `uv run python scripts/check.py` → **241 passed, 95.22%
  coverage, all checks passed** (see Gate). `uv run fathom brief AAPL` (FATHOM_AUDIT_PATH in a
  temp dir) produced a full briefing with all claims `[verified]`. `uv run fathom quote AAPL` →
  `AAPL last close 332.27 (+1.75%) as of 2026-09-11`. `uv run fathom probe` →
  `provider=offline model=extractive-v1 latency_ms=0` / `pong`. `uv run fathom --help` listed all
  documented subcommands. `uv run fathom app` / `uv run fathom api` / `uv run fathom mcp` are
  documented, not executed interactively (out of scope for AC1's targeted list).

- **AC2 — SHOWCASE embeds ≥5 PNGs + metrics table: PASS.**
  SHOWCASE.md embeds exactly 5 `<img src="assets/...">` tags (00-full-page, 01-header-quote,
  02-chart-filings, 03-briefing, 04-ask), all existing under `docs/assets/`, sizes 146362 /
  45839 / 77899 / 480041 / 70377 bytes — all ≤ 600 KB. `uv run python scripts/screenshots.py`
  regenerated all 5 PNGs cleanly (no `--group screenshots` needed; `playwright install chromium`
  plus the plain command, exactly as README §9 documents). The metrics table (with
  `<!-- metrics:start/end -->` markers) is present in both README and SHOWCASE.

- **AC3 — no invented figures: FAIL (blocking).** See Figure audit below. README's and
  SHOWCASE's metrics table row "Offline briefing latency, median" reads **36 ms**; the current
  `metrics/card.md` (regenerated by the gate, unchanged by my run) reads **33 ms**. Both docs'
  gate-quote reads "**238 passed, 95.18% coverage**"; the gate now reports **241 passed, 95.22%
  coverage** (T-013 attempt 2 landed after T-011 and added tests/coverage). These are stale
  quoted figures, not matching the current gate/metrics-card output.

- **AC4 — HF dataset table matches data/SOURCES.md: PASS.**
  README.md and docs/OVERVIEW.md both list the four datasets/licences/row-counts
  (`musk1209/finsight-sec-filings` MIT 97; `AlphaDojo/dojo_stock_kline` Apache-2.0 8480;
  `AlphaDojo/dojo_quote` Apache-2.0 20; `AlphaDojo/dojo_stock_info` Apache-2.0 20) exactly as in
  `data/SOURCES.md`.

- **AC5 — gate passes; screenshots.py lint/type-clean: PASS.**
  `uv run python scripts/check.py` → all checks passed (241 passed). `uv run ruff check
  scripts/screenshots.py` → all checks passed; `uv run ruff format --check` → already formatted;
  `uv run mypy scripts/screenshots.py` → no issues. Not on any exclude list — passes strictness
  outright, satisfying AC5 either way.

## Figure audit (quoted string → source → match?)

| Quoted string | Where | Source checked | Match? |
|---|---|---|---|
| "238 passed, 95.18% coverage" | SHOWCASE.md:14 | current gate: 241 passed, 95.22% | **NO — stale** |
| "36 ms" (Offline briefing latency, median) | README.md:104, SHOWCASE.md:173 | metrics/card.md: 33 ms | **NO — stale** |
| Parser coverage 1.0 / ratio / 1.0 / pass | README, SHOWCASE | metrics/card.md | yes |
| Golden-query retrieval hit rate 0.7786 | README, SHOWCASE, OVERVIEW, ASSUMPTIONS | metrics/card.md | yes |
| Guard escapes 0 / Guard false positives 0 | README, SHOWCASE | metrics/card.md | yes |
| Offline briefing verified share 1.0 | README, SHOWCASE, OVERVIEW ("100%"), ASSUMPTIONS ("1.0") | metrics/card.md | yes |
| Prompt-injection guard test True | README, SHOWCASE | metrics/card.md | yes |
| 4 HF dataset ids + licences + row counts (97/8480/20/20) | README, OVERVIEW | data/SOURCES.md | yes |
| "97 filings (20 10-K, 77 10-Q), filed 2025-04-23 to 2026-05-29" | OVERVIEW, ASSUMPTIONS | data/SOURCES.md Coverage section | yes |
| "8,480 rows, 2025-01-02 to 2026-09-11" | OVERVIEW, ASSUMPTIONS | data/SOURCES.md | yes |
| quote_time max "2026-09-11T16:00:01+00:00" | OVERVIEW, ASSUMPTIONS | data/SOURCES.md | yes |
| AAPL "last close 332.27 (+1.75%) as of 2026-09-11" | SHOWCASE §5 | `fathom quote AAPL` output + data/SOURCES.md anchor | yes |
| SHOWCASE `fathom brief AAPL --json` excerpt (accessions, n_chars, claim text) | SHOWCASE §5 | live `uv run fathom brief AAPL --json` re-run | yes, byte-identical except `generated_at` (expected, timestamped per run) |
| SHOWCASE `/api/ask/AAPL` envelope (question_sha256, claims, accessions) | SHOWCASE §6 | live TestClient re-run against `fathom.api.create_app()` | yes, byte-identical except timestamps |
| PNG sizes quoted in SHOWCASE §9 (142/44/76/468/68 KB) | SHOWCASE.md:192-193 | actual file sizes (143/45/76/469/69 KB rounded) | yes, within rounding |
| 20-ticker universe list | README, OVERVIEW, ASSUMPTIONS | fathom/config.py UNIVERSE (spot-checked AAPL/JPM against SOURCES anchors) | yes |

## Findings

- **CRITICAL / blocking (AC3):** README.md:104 and docs/SHOWCASE.md:14,173 quote a gate/metrics
  snapshot ("238 passed, 95.18% coverage", latency "36 ms") that predates T-013 attempt 2
  (commit 7b4e3c3), which landed after T-011's docs commit and changed both the test count and
  the offline-latency KPI. Current truth: gate reports **241 passed, 95.22% coverage**;
  `metrics/card.md` reports latency **33 ms**. Fix: update the three exact strings
  ("238 passed, 95.18% coverage" → "241 passed, 95.22% coverage"; both "36" latency-row cells
  in README.md and docs/SHOWCASE.md → "33") and re-verify no other file echoes the stale figures.
- No other findings. Scope was respected (only README.md, docs/ASSUMPTIONS.md, docs/OVERVIEW.md,
  docs/SHOWCASE.md, docs/assets/*.png, scripts/screenshots.py, plus the task-pack handoff, touched
  by 9eff1ce); pyproject.toml untouched, correctly, since the `screenshots` extra already existed.
  No invented product/compliance claims (no "SOC 2", "production-ready", etc.). Live-mode env var
  names (`FATHOM_LLM_PROVIDER`, `PORTKEY_API_KEY`, `PORTKEY_BASE_URL`, `PORTKEY_MODEL`,
  `ANTHROPIC_API_KEY`) match `fathom/config.py` exactly. Limits sections in both README and
  OVERVIEW state the 20-ticker fixture universe and the offline-by-construction caveat.

## Gate

`uv run python scripts/check.py` at current head: ruff check/format, mypy strict (host +
`--platform linux`), pytest, secrets scan, bench, bench drift, card drift all passed —
**241 passed, 95.22% coverage, no secrets found, metrics card is current, all checks passed.**
`scripts/screenshots.py` regenerates all 5 PNGs without error (142/44/76/468/68 KB, all ≤ 600 KB)
and independently passes ruff check, ruff format --check, and mypy with zero issues.

---

# Verdict T-011 — FAIL (attempt 3)

Reviewed: 3c7f737543fbd872e1c774a794d63d852a250b37  Tier: T2  Attempt: 3

## Acceptance criteria

- **AC1 — README commands run as written: PASS.** `uv sync --all-extras` succeeded (all extras
  installed, incl. streamlit/mcp/screenshots). `uv run python scripts/check.py` → **287 passed,
  96.02% coverage, all checks passed**, byte-identical to SHOWCASE.md's quoted line (see Gate).
  `uv run fathom brief AAPL` / `--json` produced the frozen `Briefing` contract, 18 claims total
  across the six sections, 18 verified (matches SHOWCASE §3's status-strip "18 claims / 18
  verified"). `uv run fathom quote AAPL` → `AAPL last close 332.27 (+1.75%) as of 2026-09-11`,
  matching README/SHOWCASE and `data/SOURCES.md`'s AAPL anchor to the cent. `uv run fathom probe`
  → `provider=offline model=extractive-v1 latency_ms=0` / `pong`. `uv run fathom --help` lists
  quote/filings/brief/ask/probe/app/api/bench/mcp — every CLI one-liner documented in README/
  SHOWCASE exists and is spelled correctly.

- **AC2 — SHOWCASE embeds ≥5 PNGs + metrics table: PASS (existence/size); prose partly stale, see
  AC3.** SHOWCASE.md embeds exactly 5 `<img>` tags (00-full-page, 01-header-quote, 02-chart-
  filings, 03-briefing, 04-ask), all existing under `docs/assets/`, current sizes 144433 /
  44095 / 77893 / 425928 / 70372 bytes — all ≤ 600 KB. The metrics table (`<!-- metrics:start/
  end -->`) is present, byte-identical, in both README and SHOWCASE.

- **AC3 — no invented/stale figures: FAIL (blocking).** See Figure audit below. The gate figures,
  metrics table, dataset table, AAPL quote, `brief --json` excerpt, and `/api/ask/AAPL` TestClient
  envelope are all current and byte-accurate re-verified against live re-runs. But SHOWCASE.md §9's
  screenshot-regeneration size line ("138 KB", "40 KB") is stale: this worktree's head is T-011
  attempt 3 **plus** the later T-017 commit ("quote-card layout — two rows of three, percentage
  delta", 3c7f737), which regenerated `docs/assets/00-full-page.png` (141340→144433 bytes) and
  `docs/assets/01-header-quote.png` (40961→44095 bytes) without anyone updating SHOWCASE.md's
  quoted sizes for them. The old sizes (138 KB, 40 KB) matched the pre-T-017 files; the current
  files are 141 KB and 43 KB. This is the same class of failure attempt 1 was blocked on — a
  quoted figure that no longer matches the artifact it describes at the reviewed commit.

- **AC4 — HF dataset table matches data/SOURCES.md: PASS.** README.md and docs/OVERVIEW.md both
  list the four datasets/licences/row-counts (`musk1209/finsight-sec-filings` MIT 97;
  `AlphaDojo/dojo_stock_kline` Apache-2.0 8480; `AlphaDojo/dojo_quote` Apache-2.0 20;
  `AlphaDojo/dojo_stock_info` Apache-2.0 20) exactly as in `data/SOURCES.md`.

- **AC5 — gate passes; screenshots.py lint/type-clean: PASS.** `uv run python scripts/check.py`
  → all checks passed (287 passed, ruff/mypy/secrets/bench/bench-drift/card-drift all green,
  "metrics card is current").

## Figure audit (string → source → match?)

| Quoted string | Where | Source checked | Match? |
|---|---|---|---|
| "287 passed, 96.02% coverage, all checks passed" | SHOWCASE.md:14 | live re-run of gate | yes |
| Full metrics table (7 KPI rows incl. latency 33 ms) | README.md:97-105, SHOWCASE.md:167-174 | `metrics/card.md` (committed) | yes, byte-identical |
| 4 HF dataset ids + licences + row counts (97/8480/20/20) | README, OVERVIEW | `data/SOURCES.md` | yes |
| "97 filings (20 10-K, 77 10-Q), filed 2025-04-23 to 2026-05-29" | OVERVIEW, ASSUMPTIONS | `data/SOURCES.md` Coverage section | yes |
| "8,480 rows, 2025-01-02 to 2026-09-11" | OVERVIEW, ASSUMPTIONS | `data/SOURCES.md` | yes |
| quote_time max "2026-09-11T16:00:01+00:00" | OVERVIEW, ASSUMPTIONS | `data/SOURCES.md` | yes |
| AAPL "last close 332.27 (+1.75%) as of 2026-09-11" | SHOWCASE §5 | live `fathom quote AAPL` + `data/SOURCES.md` anchor | yes |
| `brief AAPL --json` excerpt (accessions, n_chars, claim text, no stray "Business\nCompany Background\n" prefix) | SHOWCASE §5 | live re-run of `fathom brief AAPL --json` | yes, byte-identical except `generated_at` (expected) |
| Briefing status strip "18 claims / 18 verified" | SHOWCASE §3 | live `brief AAPL --json`, counted across all 6 sections | yes (18/18) |
| `/api/ask/AAPL` envelope (claim text, question_sha256, guard_hits 0, not_found false) | SHOWCASE §6 | live TestClient re-run against `fathom.api.create_app()` | yes, byte-identical except timestamps |
| Quote-card prose ("six `st.metric` tiles ... single shared caption") vs. current layout | SHOWCASE §2 | `docs/assets/01-header-quote.png` (T-017 layout, opened directly) | yes — two rows of three tiles, one caption, matches prose |
| Screenshot sizes "138 KB / 40 KB / 76 KB / 415 KB / 68 KB" | SHOWCASE.md:192-193 | actual current file sizes: 144433 / 44095 / 77893 / 425928 / 70372 bytes = 141 / 43 / 76 / 415 / 68 KiB | **NO — stale for 00-full-page (138→141 KB) and 01-header-quote (40→43 KB)**; 02/03/04 still round to the quoted value |
| Env var names (`FATHOM_LLM_PROVIDER`, `PORTKEY_API_KEY`, `PORTKEY_BASE_URL`, `PORTKEY_MODEL`, `ANTHROPIC_API_KEY`, `FATHOM_AUDIT_BODIES`) | README "Live mode", "Why it is trustworthy" | `fathom/config.py Settings.from_env` | yes |
| README "What's inside" links (assurance-report.md, ops/{runbook,orr,change-record,demo-checklist}.md, pitch/fathom-pitch.pptx + .png, graph/README.md) | README.md:140-148 | file existence on disk | yes, all present and non-empty |

## Findings

- **CRITICAL / blocking (AC3):** `docs/SHOWCASE.md:192-193` quotes screenshot sizes ("138 KB",
  "40 KB") for `docs/assets/00-full-page.png` and `docs/assets/01-header-quote.png` that predate
  commit 3c7f737 (T-017, "quote-card layout — two rows of three, percentage delta"), which is part
  of this reviewed head and regenerated exactly those two PNGs (plus 02-chart-filings.png, whose
  size still rounds to the same quoted "76 KB"). Current truth: 00-full-page.png is 144433 bytes
  (141 KB, not 138), 01-header-quote.png is 44095 bytes (43 KB, not 40). Fix: update the two exact
  numbers in SHOWCASE.md's §9 size line ("138 KB" → "141 KB", "40 KB" → "43 KB") and re-verify no
  other file echoes the stale byte counts (grep for "138 KB" and "(40 KB)" across docs/).
- No other findings. Scope was respected for the docs under review (README.md, docs/OVERVIEW.md,
  docs/SHOWCASE.md, docs/ASSUMPTIONS.md, docs/assets/*.png only changed by T-011 4dbc1d1 and the
  later, separately-packed T-017 3c7f737 — the latter's non-doc changes, `app/components/
  rendering.py` and `tests/test_app.py`, are outside T-011's scope and outside this pack).
  SHOWCASE's quote-card prose (six tiles, single shared caption) was correctly updated in attempt
  3 and still matches the current two-rows-of-three layout after T-017's rendering change — no
  drift there. No invented product/compliance claims (`SOC 2`, `production-ready`, `HIPAA` all
  absent from the four docs). Env var names match `fathom/config.py` exactly. Limits sections
  state the 20-ticker fixture universe and the offline-by-construction caveat consistently across
  README, OVERVIEW and ASSUMPTIONS. All nine README "What's inside" links resolve to existing,
  non-empty files.

## Gate

`uv run python scripts/check.py` at current head (after `uv sync --all-extras`): ruff check/
format, mypy strict (host + `--platform linux`), pytest, secrets scan, bench, bench drift, card
drift all passed — **287 passed, 96.02% coverage, no secrets found, metrics card is current, all
checks passed.** This matches SHOWCASE.md's quoted gate line exactly. Live re-runs of
`fathom brief AAPL --json`, `fathom quote AAPL`, `fathom probe`, and a `TestClient` call against
`fathom.api.create_app()` for `/api/ask/AAPL` all reproduced the values quoted in SHOWCASE.md
(aside from expected per-run timestamps). The one figure that does not reproduce is the screenshot
byte-size line in SHOWCASE.md §9, which is stale for 2 of 5 files — see Findings.
