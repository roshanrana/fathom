# Security review T-013 — CLEAR
Boundary: B1, B3  Risk class: medium

Closure: T-009 F1 CLOSED — `fathom/cli.py` `run_api` now `host: HostOption = "127.0.0.1"`
(uvicorn.run uses the passed `host`, no hardcoded `0.0.0.0`); `--host 0.0.0.0` requires an
explicit flag, matching LLD §4's frozen `fathom api [--host 127.0.0.1] [--port 8000]` and D-008.
T-009 F2 CLOSED — `AskBody.question: str = Field(min_length=1, max_length=2000)` in
`fathom/api.py` (422 beyond 2 000, confirmed by `tests/test_api.py`
`test_fr014_post_ask_question_over_2000_chars_returns_422` / `..._at_2000_chars_is_accepted`,
both passing); CLI `ask` (`fathom/cli.py`) rejects `len(question) > 2000` with exit 2 before
calling `ask_flow`.

1. [LOW] CLI `ask` (`fathom/cli.py:146-153`) enforces the 2 000-char upper bound but not
   `AskBody`'s `min_length=1` — an empty string reaches `ask_flow` unblocked. No security impact
   observed (guard/retrieval/provider all handle empty text safely); flagging only for
   consistency with the API's bound. B1 — informational, not blocking.

Checks confirmed clear (no finding):
- All 14 `ADVICE_PATTERNS` (`fathom/guard.py`) timed on 200 KB adversarial-repetition inputs:
  "should be be be buying" x8000 → 0.094 s; "recommend " x20000 → 0.109 s; "a" x200000 →
  0.029 s. No catastrophic backtracking; well under 1 s (LLD §6.4 amended patterns 1, 3, 9, 11).
- No new false positives: LLD §6.4's 5-phrase benign set plus 10 additional realistic filing
  sentences I wrote (e.g. "We hold investments in marketable securities.", "The Company should
  be able to fund operations from existing cash and cash equivalents.", "Sell-side analysts are
  not engaged by the Company.") all return `is_advice() == False`. Pattern 9's `(i|you|we|...)`
  alternation does not include "be", so "should be able" does not collide with the new `\w*`
  inflection suffix on patterns 1/9.
- API: `run_api` default host `127.0.0.1`, `--host` explicit for `0.0.0.0` (B1, C-08 in threat
  model terms). `AskBody.question` bounded 1-2000; CLI `ask` bounded ≤2000 (see closure above).
- Parser fallback (`fathom/filings.py` `_heading_vocabulary_matches`/`_fallback_candidate`/
  `_apply_heading_fallback`) and retrieval title-boost (`fathom/retrieval.py`
  `CANONICAL_SECTIONS.get(chunk.section_id, "")`) use only `re`/`str` slicing/`tokenize` (regex
  `\w+`) — no `eval`/`exec`/`str.format`/f-string built from filing text; filing text is treated
  purely as data (B3, C-22). Measured `parse_sections` on the largest fixture (JPM 10-K,
  accession 0001628280-26-008131, 1,178,161 chars) at 0.057 s (worst of all 97 fixtures: 0.058 s,
  total corpus 1.33 s) — no O(keys × lines × candidates) blow-up observed, far under the 5 s
  threshold.
- `fathom/bench.py`: `_briefing_offline_stats` and `_injection_check` write only under
  `tempfile.TemporaryDirectory()` audit paths; `run_bench` writes only `out` (default
  `metrics/headline.json`) and `out.parent / "timing.json"`. No other filesystem writes in the
  diff. (The `.gitignore` addition of `.tmp-audit/` is unused by any code in this diff — no
  finding.)
- No new dependencies: `pyproject.toml`/`uv.lock` untouched by this commit (`git show HEAD --
  pyproject.toml uv.lock` empty); `uv sync --all-extras` resolved the existing 80 packages with
  no changes.
- Full test suite for touched modules (`test_guard.py`, `test_api.py`, `test_filings.py`,
  `test_retrieval.py`, `test_bench.py`): 111 passed.

Scanner summary: secrets 0, ruff clean (`uv run ruff check fathom` → "All checks passed!"),
deps unchanged: yes
