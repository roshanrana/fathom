# Security review T-022 — CLEAR
Boundary: B6  Risk class: medium

Closure: T-018 F1 CLOSED — `_validate_accession`/`_validate_safe_name` (sec.py:70-90) are called in
`_to_filing` on `accession`/`primary_document` (sec.py:277-278) and in `filings()`'s older-pages
loop on `name` (sec.py:218), in every case before the value reaches `_document_url`/
`_SUBMISSIONS_PAGE_URL.format`; `_ACCESSION_RE` (`^\d{10}-\d{2}-\d{6}$`) and `_SAFE_NAME_RE`
(`^[A-Za-z0-9][A-Za-z0-9._-]{0,120}$` + explicit `".." in name`) jointly reject `..`, `/`, `\`,
`%`, whitespace, and names over 121 chars; failure raises `SOURCE_HTTP` reason "invalid edgar
field" without echoing the value (verified by grep — no interpolation of the raw value into the
message/details). CIK stays zero-padded digits via `f"{int(cik_str):010d}"` (sec.py:189,
byte-for-byte unchanged from T-018) and is the only EDGAR-derived value reaching
`_submissions`/`company_concept` URLs — `taxonomy`/`concept` there are hardcoded literals in
facts.py, not attacker-controlled. Tests `test_fr021_filings_rejects_invalid_{accession_number,
primary_document,older_page_name}` (test_live_sec.py:306-414) exercise all three sites with
`../../etc`-style payloads and pass.
F2 CLOSED — `LiveHttp.get` (http.py:57-107) now opens `self._client.stream("GET", …)` as a `with`
context manager, checks `200 <= status < 300` before any body read, then accumulates
`response.iter_bytes()` into a `bytearray`, raising "too large" the moment it exceeds 25 MB —
strictly before `.decode()` (which only happens later, in `sec.py`'s `document_text`, on the
already-capped bytes). The abort happens inside the `with` block, so `__exit__` still closes the
stream/response on the `FathomError` path — no leak. `_write_cache` (line 106) is reached only
after the loop completes without raising, so cache writes remain whole-body-only. Confirmed by
`test_fr025_streamed_30mb_body_aborts_after_26mb_read_no_text_decode` (reads ≤26 MB of a 30 MB
stream) and `test_fr025_streamed_20mb_body_succeeds`.
F3 CLOSED — the `httpx.HTTPError` branch (http.py:98-103) now sets
`"reason": type(exc).__name__`; grep of `fathom/live/http.py` finds no remaining `str(exc)`.
`test_fr025_transport_error_reason_is_exception_class_name_no_url` asserts the reason is exactly
`"ConnectError"` and that neither the message nor details contain the URL/secret embedded in the
raised exception.

Regressions: none. `_throttle`/`_THROTTLED_HOSTS`/`_THROTTLE_INTERVAL_S` (per-host, 0.12 s) are
untouched by this diff (confirmed via `git show HEAD`); cache-key scheme (`sha256(url)` under
`_http/`) and `.meta` shape are untouched; `pyproject.toml`/`uv.lock` have zero diff in this commit
(no new dependencies). No new findings identified.

Scanner summary: secrets 0, ruff clean (fathom/live — "All checks passed!"), deps unchanged: yes
(targeted suite: `uv run pytest tests/test_live_http.py tests/test_live_sec.py -q` — 35 passed)
