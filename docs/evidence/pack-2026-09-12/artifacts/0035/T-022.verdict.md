# Verdict T-022 — PASS
Reviewed: df6a07aad19e00a68b757aac6875143e3d2b7030  Tier: T2  Attempt: 1

## Acceptance criteria
- AC1 (invalid edgar field): PASS. Independently crafted submissions JSON with `accessionNumber: "../../etc"`, with `primaryDocument: "../x.htm"`, and with an older-page `files[].name` of `"../../etc/passwd"` each raise `SOURCE_HTTP` with `details["reason"] == "invalid edgar field"`; the raw value appears in neither `message` nor `details` (checked via substring search on both). `_validate_accession`/`_validate_safe_name` (fathom/live/sec.py:83-90) run before `_document_url`/`_SUBMISSIONS_PAGE_URL.format` in `_to_filing` and the `filings()` older-pages loop. The real `aapl_submissions.json` fixture still parses unchanged (5 filings returned).
- AC2 (streamed size cap): PASS. Independent MockTransport built from a counting generator: a 30 MB body aborts with reason "too large" after reading ≤ 26 MB (worst case 25 MB + one 1 MB chunk, matches the frozen ≤26 MB bound); a 20 MB body succeeds and returns the full body. `LiveHttp.get` (fathom/live/http.py:67-97) uses `client.stream(...)` + `response.iter_bytes()`, comparing running `len(buffer)` — no `.text`/`.content` access anywhere before the cap check.
- AC3 (reason sanitisation): PASS. `httpx.ConnectError("http://host/secret?x=1")` yields `details["reason"] == "ConnectError"` with neither the URL nor "secret" present in message or details (fathom/live/http.py:98-103, `type(exc).__name__`).
- AC4 (signatures + tests + gate): PASS. `git diff 773bb2b HEAD -- fathom/live/http.py fathom/live/sec.py`: every public `def` line (`LiveHttp.__init__`, `.get`, `.close`; `SecClient.__init__`, `.from_settings`, `.ticker_map`, `.lookup`, `.filings`, `.document_text`, `.company_concept`) is byte-identical to base commit 773bb2b — only new private helpers (`_validate_accession`, `_validate_safe_name`, `_invalid_edgar_field_error`) were added. New tests are named `test_fr021_*`/`test_fr025_*`. Full gate: 345 passed, ruff/format/mypy(x2)/secrets/bench/card all green. Targeted suite: 35 passed. `uv run mypy fathom`: no issues in 23 files.

## Findings
None. Scope respected — diff touches only `fathom/live/http.py`, `fathom/live/sec.py`, `tests/test_live_http.py`, `tests/test_live_sec.py`, and a 6-line Handoff addition to the pack itself. All three T-018 findings closed as specified: F1 validates `accessionNumber`/`primaryDocument`/older-page `name` before any URL is built; F2 streams via `client.stream`+`iter_bytes` and aborts on the running total before any decode; F3 replaces `str(exc)` with `type(exc).__name__` in the transport-error branch. No URL or filing-field value is echoed in any `SOURCE_HTTP` message/details path exercised.

## Gate
`uv sync --all-extras`: OK. `uv run pytest tests/test_live_http.py tests/test_live_sec.py -q`: 35 passed. `uv run mypy fathom`: no issues (23 files). `uv run python scripts/check.py` (HTTPS_PROXY=http://127.0.0.1:9): 345 passed, coverage 95.92% (≥80% required), ruff/format/mypy/mypy-linux/secrets/bench/bench-drift/card-drift all passed — "all checks passed".
