# Verdict T-021 — PASS
Reviewed: 36a9a02961cbba66e97391d325cf267bfc6c800e  Tier: T2  Attempt: 1

## Acceptance criteria
- AC1 — PASS. Without env vars, `uv run pytest tests/test_live_network.py -q` → `2 skipped` (confirmed).
  With `FATHOM_NETWORK_TESTS=1 FATHOM_SEC_CONTACT=fathom-verifier@example.com`, the same test → `2 passed`
  (see Network run below). Gate stays offline without the env vars (confirmed via `check.py` run under a
  dead HTTPS_PROXY — see Gate).
- AC2 — PASS. Ran the README "Live data" section verbatim (placeholder contact, temp
  `FATHOM_LIVE_CACHE_DIR`): `fathom fetch NFLX --force` and `fathom brief NFLX` both ran clean and
  reproduced the same filings/sections/bars-range/snapshot-fields shape pasted in `docs/SHOWCASE.md`
  §9 (fetched_at timestamp naturally differs run to run). Every other figure quoted in docs
  (filing counts/dates, bars range, snapshot fields, cold-start seconds, gate pass/skip/coverage
  counts) traces to a command actually run — see Figure audit.
- AC3 — PASS. `git diff 08096ac..36a9a02 -- README.md` shows the "Live data" section is a pure
  insertion; the Metrics block (lines ~133-153, `<!-- metrics:start -->`...`<!-- metrics:end -->`)
  is byte-for-byte unchanged. README, ASSUMPTIONS.md and SHOWCASE.md §9 all state Yahoo/Stooq are
  unofficial endpoints and that XBRL-derived ratios are approximations, not vendor figures.
- AC4 — PASS. `uv run python scripts/check.py` with `HTTPS_PROXY=http://127.0.0.1:9` (unreachable
  proxy, forces any stray network call to fail fast) → all checks passed, 428 passed / 2 skipped,
  95.54% coverage (gate's own bench/coverage figures, not doc-quoted — see Findings for the one
  place a stale count exists elsewhere in the repo, out of this task's scope).

## Network run (tickers, seconds, coverage)
`FATHOM_NETWORK_TESTS=1 FATHOM_SEC_CONTACT=fathom-verifier@example.com uv run pytest
tests/test_live_network.py -q -s` → **2 passed** in 5.24s.
- NFLX cold materialize: 2.25s (docs quote 2.58s from the implementer's own run — expected to
  differ machine/date to machine/date; NFR-012 is explicitly "measured... and reported, not
  gated", and both figures are well under the 30s budget).
- COST cold materialize: 2.01s (docs quote 2.38s, same caveat).
- Both tickers: 10-K parser coverage has `1A`+`7`; every 10-Q has `I.2`; bars ≥ 250 rows;
  `quotes.market_cap` present and non-null; offline `brief()` on the freshly materialized cache →
  `verified_share == 1.0`. All per-AC assertions in `tests/test_live_network.py` held.
- Placeholder SEC contact (`fathom-verifier@example.com`) was accepted without incident by
  `SecClient`/live mode — SEC's fair-access policy asks for *a* contact string, it does not
  validate the address, as the pack anticipated.

## Figure audit
- `docs/SHOWCASE.md` §9 `fathom fetch NFLX --force` output (filings/accessions/sections, bars
  range `2024-09-12..2026-09-11`, snapshot fields) — reproduced verbatim (modulo `fetched_at`) by
  my own run of the same command with a placeholder contact and temp cache dir.
- Cold-start seconds (NFLX 2.58s / COST 2.38s in docs) — labeled via the test file name and
  compared against the ≤30s NFR-012 budget, not asserted as a guarantee; consistent with my
  measured 2.25s / 2.01s on this run. No discrepancy beyond expected run-to-run variance.
- README "Live data" section: SEC fair-access throttle (≤8 req/s vs 10/s cap), TTL (6h), cache
  path (`.cache/live/<TICKER>/`) all match `05-m4-live-data.md` §1/§3 exactly.
- `docs/design/02-threat-model.md`: B6 boundary row added (line 30) and four B6 STRIDE rows (T,
  D, I, R) added, each citing `05-m4-live-data.md §2` — confirmed present and correctly cited.
  B6 boundary description also matches `05-m4-live-data.md` §2's B6 paragraph.
- `docs/ops/demo-checklist.md`: has a "Live data mode" beat (§ "optional extra beat, T-021") with
  explicit fallback lines for both a live-call failure and a live-data-fetch failure.
- `docs/ops/runbook.md`: "Live data mode" section with start command, symptom/cause/action table,
  and explicit fallback (`unset FATHOM_DATA_SOURCE` / `--source fixture`) — confirmed.
- `docs/OVERVIEW.md` data-sources table gained exactly one live row (git diff confirms 1-line
  addition); `docs/ASSUMPTIONS.md` gained the "Live mode caveats" section (Yahoo/Stooq unofficial,
  XBRL approximations, 6h TTL) — confirmed via diff, nothing else in either file altered.
- Gate pass/coverage counts: my own run (428 passed / 2 skipped, 95.54% coverage) differs from the
  handoff's figure (420 passed, 96%+ coverage) and from `docs/SHOWCASE.md` §1's older figure (287
  passed, 96.02% coverage, predating this task). None of these exact numbers are asserted in the
  T-021-scoped docs (README/OVERVIEW/ASSUMPTIONS/SHOWCASE §9/runbook/demo-checklist) — they only
  appear in the handoff (not a doc) and in SHOWCASE §1 (out of this task's scope, stale from an
  earlier task as the suite has grown since). Flagged as a finding, not an AC failure.

## Findings
- No personal e-mail address found in any tracked file. `git grep -nE
  "[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[a-z]{2,}"` returns only `<your.name@example.com>` (README,
  SHOWCASE, test docstring) and `t@example.com` / `ops@example.com` / `fathom-verifier@example.com`-
  style test fixtures — all placeholders, consistent with the Orchestrator's stated redaction of
  the handoff contact before commit.
- LOW: `docs/SHOWCASE.md` §1's gate summary ("287 passed, 96.02% coverage") is stale relative to
  the current suite (428 passed, 95.54% coverage after this task's new test file). §1 is outside
  T-021's touched scope (only §9 was in scope) and was already stale before this task — noting for
  the record, not a T-021 defect.
- INFO: handoff's gate figure ("420 passed/2 skipped, 96%+ coverage") also doesn't match my
  428/95.54% run; harmless drift (suite grew / coverage shifted slightly between the implementer's
  run and mine), not doc-asserted, no AC depends on the exact count.
- No other findings. Scope discipline confirmed via `git diff 08096ac..36a9a02 --stat`: exactly
  the 8 files/dirs listed in the pack's Scope section were touched (README.md, ASSUMPTIONS.md,
  OVERVIEW.md, SHOWCASE.md, 02-threat-model.md, demo-checklist.md, runbook.md, plus the task pack
  itself and tests/test_live_network.py); the optional screenshot was not added, and SHOWCASE.md
  §9 explains why (`scripts/screenshots.py` has no hook to switch to the live sidebar radio).

## Gate
Offline gate green: `uv run python scripts/check.py` under `HTTPS_PROXY=http://127.0.0.1:9`
(unreachable) → ruff, mypy (host + linux), pytest, secrets scan, bench, bench drift, card drift
all passed — 428 passed, 2 skipped, 95.54% coverage, "all checks passed".
