# Verdict T-007 — PASS
Reviewed: d134edbffb4ca3d17615a60a423c9c8a8f8113de  Tier: T2  Attempt: 1

## Acceptance criteria
- AC1 (advice guard): PASS. `ask("AAPL", "Should I buy Apple?", settings, provider=FailingProvider())` → `guard_hits==1`, one claim with `guarded=True`, `verified=False`, text = `GUARD_NOTICE`, `not_found is False`, `provider=="guard"`, `model=="-"`; provider never invoked; audit line `provider=="guard"`, `model=="-"`. Verified independently with a temp audit path, not just via the bundled test.
- AC2 (nonsense/zero-hit): PASS. `ask("AAPL", "zqxjv wvutsr", settings, provider=FailingProvider())` → `not_found is True`, `claims==[]`, `provider=="none"`; provider never invoked; audit `provider=="none"`. Verified independently.
- AC3 (offline end-to-end): PASS. `ask("AAPL", "What are the main risk factors?", Settings(audit_path=tmp))` (real AAPL fixtures, no provider override → offline extractive provider) → ≥1 claim, all `verified is True`, all `source.section_id` in the canonical 10-K/10-Q id set, `not_found is False`. Verified independently.
- AC4 (verify against full section, not chunk): PASS. Built a scripted draft citing a real qualifying sentence from `10-K:1A` that is absent from a fabricated retrieved-chunk text (retrieval monkeypatched to return that fake chunk); the returned claim verified `True` — confirms verification runs against `sections_for(...)` full text (`fathom/ask.py` `_section_text_index` / `_claims_from_draft`), not the chunk.
- AC5 (repair path): PASS. Invalid-then-valid script → 2 provider calls, second call's user JSON carries a `"repair"` key, draft accepted (`not_found is False`, one claim with `verified is False` for an unknown accession — correct, since an unrecognized accession/section_id yields no section-text match). Invalid-twice script → raises `FathomError(Code.CONTRACT_INVALID)` with `details["attempt"]==2` after exactly 2 calls.
- AC6 (never logged/audited) + mypy/test-naming: PASS. A capturing handler on logger `fathom` (level DEBUG) sees no record containing an embedded nonce, across both the guard path and the zero-hit path, with `audit_bodies=True`; the audit file contains no occurrence of the nonce; audit lines carry only `prompt_sha256`/`response_sha256` (no raw question, no `question` key) for the no-call paths. `uv run mypy fathom` (strict, per `pyproject.toml` `strict = true`) → clean. All 7 tests in `tests/test_ask.py` are named `test_fr009_*`.

## Verification checklist
- [x] scope respected — diff touches only `fathom/ask.py` (new), `tests/test_ask.py` (new), `docs/tasks/T-007-ask.md` (Handoff section).
- [x] error taxonomy used — `Code.CONTRACT_INVALID` raised with `{"attempt": 2}` per §7; `_section_text_index` catches `FathomError` per-accession rather than propagating, consistent with §2.10's "unknown accession/section_id → verified False" rule (noted as a deliberate, documented deviation in the Handoff, and reasonable — a bad accession among several hits shouldn't abort the whole `ask()` call).
- [x] question text never logged or audited — true for the no-call paths (guard, zero-hit) regardless of `audit_bodies`, matching AC6 as specified. Note: when `audit_bodies=True` **and** the flow reaches the provider (retrieval hits + a real/scripted call), the stored `prompt` field is the full user JSON, which embeds the raw `question` field — I confirmed this by independent execution (a real question + `audit_bodies=True` produces an audit line containing the question). This is **not** a defect: `docs/design/01-requirements.md` NFR-006 scopes the never-log/audit guarantee to "(default mode)", and `docs/design/02-threat-model.md` B4 explicitly documents "opt-in mode stores bodies by design" as an owner-accepted risk. `docs/tasks/T-007.security.md` already surfaced and accepted this exact behavior. Flagging only so the next reader isn't surprised by the task-pack summary sentence ("the question text itself is never logged or audited") being narrower than the actual frozen requirement.
- [x] tests named with RTM IDs — all 7 tests in `tests/test_ask.py` are `test_fr009_*` (rtm: FR-009 per the pack's front-matter).
- [x] no new dependencies — no diff to `pyproject.toml` or `uv.lock` in this commit.

## Findings
- [INFO] `docs/tasks/T-007.security.md` already reviewed this diff (boundary B1/B4, risk medium) and recorded two accepted candidates (MEDIUM: `SYSTEM_ASK` doesn't yet tell the model the `question` field is untrusted data, mitigated by guard-first + `verify_claim` + `scrub_claims`; LOW: `Claim.quote` on a guarded claim is left untouched, open pending a future render-layer confirmation). Nothing new found here that contradicts or supersedes that review.
- [INFO/non-blocking] The stray `.tmp-audit/log.jsonl` directory in the live tree (`C:\Code-Central\fathom\.tmp-audit\`) was **not** created by this task: its single line has `"purpose": "brief"`, which `fathom/ask.py`/`tests/test_ask.py` never write (`ask()` always audits `purpose="ask"`). It originates from `briefing.py`/T-006 work elsewhere in this wave, not from T-007's diff or tests. `tests/test_ask.py` uses only `tmp_path`-rooted audit paths.
- `graphify affected "ask" --depth 2` shows impact confined to `tests/test_ask.py`'s own test functions — no impact outside the pack's scope.

## Gate
- `uv run pytest tests/test_ask.py -q` → 7 passed.
- `uv run ruff check fathom/ask.py tests/test_ask.py` → all checks passed.
- `uv run mypy fathom` → success, no issues (strict).
- `uv run python scripts/check.py` (full gate) → all checks passed: format (48 files), ruff, ruff format --check, mypy (default + `--platform linux`), pytest (165 passed, 97.08% coverage, ≥80% required), secrets scan clean.
