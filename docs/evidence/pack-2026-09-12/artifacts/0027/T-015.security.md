# Security review T-015 — CLEAR
Boundary: B2  Risk class: medium
Closure: T-004 F1 CLOSED — `_parse_portkey_payload`/`_parse_anthropic_payload` (providers.py:92-115) catch `(json.JSONDecodeError, KeyError, IndexError, TypeError)` around the envelope index/subscript chain and raise `PROVIDER_HTTP`/"malformed response"; probed with an independent `httpx.MockTransport` (object, list, null, string, number, bool, non-JSON text, empty body) against both providers — every case raised `FathomError`, none leaked the body or key, none escaped as raw `KeyError`/`IndexError`.
D-010 G7 finding CLOSED — `except httpx.HTTPError` now wraps connect/protocol/DNS errors as `PROVIDER_HTTP` with `reason=<exception class name>` (never `str(exc)`, so no URL leak); reproduced live via `PORTKEY_BASE_URL=http://127.0.0.1:9/v1 fathom brief AAPL` → `error PROVIDER_HTTP: portkey provider request failed (reason=ConnectError)`, exit 2, no raw traceback.
1. [LOW] `tests/test_providers.py`'s malformed-body matrix covers missing/empty dict shapes and invalid JSON text but not top-level list/null/string/number/bool — a coverage gap only; my own probe confirms the implementation already handles all of these correctly (control C-09) — no fix required, optional test addition.

Verified clean: no `except Exception`/bare `except` in providers.py/cli.py; `reason` is always a fixed string or `type(exc).__name__`, never `str(exc)`; grep + probe show the API key never appears in any message, `details`, or the one `logger.info("provider selected ...")` line; `cli.py` diff is confined to `_claim_line`'s new `if not claim.source.accession` branch (rendering only — matches the existing `Source(accession="", ...)` sentinel from `ask.py`'s input-guard notice, no new I/O/logic); no `pyproject.toml`/`uv.lock` change; 51/51 tests in `test_providers.py`+`test_cli.py` pass.
Scanner summary: secrets 0, ruff 0/0 (providers.py, cli.py — all checks passed), deps unchanged: yes
