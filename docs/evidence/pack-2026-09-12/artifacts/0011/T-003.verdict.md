# Verdict T-003 — PASS
Reviewed: 3f08cdb5229a4dcdb90940979ca827955eb7830a  Tier: T2  Attempt: 1

## Acceptance criteria
- AC1 PASS: `tokenize("The Company's risk factors, and its 10-K")` independently re-executed → `["risk", "factors", "10"]`, exact match.
- AC2 PASS: synthetic 30-sentence section (size=1200/overlap=200) → 2 chunks; all `len(text) <= 1200`; ordinals `0..n-1`; `doc_id == f"{accession}#{section_id}#{ordinal}"`; every consecutive pair shares ≥1 sentence. Hard-cut and empty-text cases also covered by dedicated tests (`test_fr009_chunk_section_hard_cuts_an_over_long_sentence`, `test_fr009_chunk_section_empty_text_yields_no_chunks`) and pass.
- AC3 PASS: three-doc toy corpus, query "widget" → only `doc_b` returned, score 0.9808 > 0, docs with score 0 excluded. Independently re-verified plus separate tie-break case (`doc_z`/`doc_a`, identical text) → order `["doc_a", "doc_z"]` with equal scores, confirming doc_id-ascending tie-break.
- AC4 PASS: `search("AAPL","risk factors",6,data_dir)` top-1 section = `10-Q:II.1A` (in allowed set); `search("AAPL","liquidity and capital resources",6,data_dir)` top-1 = `10-Q:I.2` (in allowed set); `search("AAPL","zqxjv",6,data_dir) == []`. All independently re-executed, not just re-run via pytest.
- AC5 PASS: `index_for("AAPL", data_dir)` called twice → `first is second` (functools.cache hit); `len(first.chunks) == 280 > 100`.
- AC6 PASS: `uv run mypy fathom` → "Success: no issues found in 9 source files"; all test functions in tests/test_retrieval.py are named `test_fr009_*`.

Additionally verified against frozen LLD §2.6 (not merely re-run):
- `STOPWORDS` set-equality against the exact LLD-listed 47 words: match (no diff).
- idf formula: `self._idf("widget")` recomputed independently as `math.log((N-n+0.5)/(n+0.5)+1)` → identical value.
- Chunking rule (sentence-boundary split via `re.split(r"(?<=[.!?])\s+", ...)`, accumulate-until-exceeds-size, overlap-seeded continuation, hard-cut oversized sentence) matches spec prose and is exercised by both the general and hard-cut tests.

## Verification checklist
- [x] Scope respected — diff touches only `fathom/retrieval.py` (new) and `tests/test_retrieval.py` (new), plus the pack file itself (status/handoff fields).
- [x] No new dependencies — imports are `functools`, `math`, `re`, `collections.Counter`, `pathlib.Path`, `pydantic.BaseModel`, and in-package `fathom.filings`. No `rank_bm25`, `sklearn`, or `numpy`.
- [x] Deterministic ordering — `search`/`BM25Index.search` sort by `(-score, doc_id)`; confirmed via tie-break test and independent re-execution.
- [x] Tests named with RTM IDs — all 11 tests in `tests/test_retrieval.py` are `test_fr009_*`, module docstring cites `RTM: FR-009`.
- [x] No filing text logged — no `logging`/`print` calls anywhere in `fathom/retrieval.py`.

## Findings
None blocking. No CRITICAL/HIGH/MEDIUM issues found.

## Gate
`uv run python scripts/check.py` → **all checks passed** (ruff check, ruff format --check, mypy fathom, mypy fathom --platform linux, pytest, secrets scan all green on this commit's tree, which contains T-000..T-004 only — the handoff's note about a pre-existing `ruff format --check` failure on `fathom/guard.py` no longer reproduces at this commit).
Test count: 84 passed (11 in `tests/test_retrieval.py`, targeted `pytest tests/test_retrieval.py -q` → 11 passed).
Coverage: 96.97% total (required 80%); `fathom/retrieval.py` 99% (1 line uncovered: line 207, the zero-`avgdl`/empty-index guard branch).
