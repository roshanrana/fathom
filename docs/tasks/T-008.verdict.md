# Verdict T-008 — PASS
Reviewed: e3f04b7085ad391f54431c7d49c8d0ebd4fa82a3  Tier: T2  Attempt: 1

## Acceptance criteria
- AC1: PASS. `AppTest.from_file("app/main.py", default_timeout=120).run()` in offline mode (`FATHOM_LLM_PROVIDER=offline`, `FATHOM_DATA_DIR=<repo>/data`, `FATHOM_AUDIT_PATH=<tmp>/audit/fathom-audit.jsonl`): no exception, 1 selectbox, 5 `st.metric` elements, 1 plotly chart, 1 dataframe, disclaimer text (`fathom.config.DEFAULT_DISCLAIMER`) present in a caption. Verified independently with a standalone script (not the bundled test file).
- AC2: PASS. Clicking `sidebar.button[0]` ("Generate briefing") renders the six subheaders `Business snapshot, Latest results, Risks, Liquidity and capital, Notable disclosures, Talking points` in that exact order; ≥10 claim-bullet markdown lines, every one ending in one of the three badge strings; ≥10 `Source:` captions, each carrying a non-empty accession segment. Verified independently.
- AC3: PASS. Asking "What are the main risk factors?" → at least one claim line ending `✅ verified`. Asking "Should I buy?" → a markdown block containing `fathom.guard.GUARD_NOTICE`. Verified independently.
- AC4: PASS. Selecting AAPL and generating a briefing calls a monkeypatched counting wrapper around `fathom.briefing.make_provider` exactly once; switching to MSFT changes `at.header[0].value`; switching back to AAPL restores the same header text and the call counter stays at 1 (no re-invocation for the cached ticker). Verified independently, replicating the implementer's monkeypatch technique from scratch.
- AC5: PASS. Before any briefing, the status caption starts `Provider: offline` with `Model: -`. After clicking "Generate briefing", the last such caption contains `Model: extractive-v1` and both the substrings `claims` and `verified` (rendered as `claims <N> · verified <M>`).
- AC6: PASS. `grep`/directory-walk over `app/**/*.py` for `read_parquet`, `BM25`, `re.compile` → no matches. Separately grepped for `unsafe_allow_html=True` → no matches (threat-model boundary B1 requirement). All 8 tests in `tests/test_app.py` are named `test_fr012_*` / `test_nfr011_*` (rtm: FR-012, NFR-011 per the pack's front matter).

## Verification checklist
- [x] scope respected — `git show HEAD --stat` touches only `app/main.py`, `app/components/__init__.py`, `app/components/badges.py`, `app/components/rendering.py`, `tests/test_app.py` (new), `tests/test_app_skeleton.py` (deleted), and `docs/tasks/T-008-streamlit-page.md` (Handoff section). No changes under `fathom/`; `pyproject.toml` has no diff in this commit.
- [x] only `FathomError` caught — the only three `except` clauses in `app/` (all in `app/main.py`, lines 48/56/65) catch `FathomError`; `app/components/*.py` contain no `except` at all.
- [x] disclaimer present; badge text carries state — `st.caption(settings.disclaimer)` renders `DEFAULT_DISCLAIMER`; `app/components/badges.py` maps `guarded → "⛔ removed"`, `verified → "✅ verified"`, else `"⚠️ unverified"` — three fixed text strings, never colour-only (NFR-011 test also confirms exactly one known badge per claim line).
- [x] tests named with RTM IDs — `test_fr012_ac1_*` … `test_fr012_ac6_*`, `test_nfr011_*`.
- [x] no new dependencies — `pyproject.toml` unchanged by this commit; `streamlit>=1.38` and `plotly>=5.22` were already declared dependencies (confirmed against the constraint list in `01-requirements.md` §Constraints).

## Findings
- [LOW/non-blocking] `render_chart` (`app/components/rendering.py:92`) passes `use_container_width=True` to `st.plotly_chart`, which streamlit 1.63 flags as deprecated ("replace with `width='stretch'`", removal after 2025-12-31). Harmless today and outside this task's failure modes, but will need updating before that removal date.
- No CRITICAL/HIGH issues found. The Handoff's self-reported "full gate FAILS outside my scope" note is stale for this commit: per the task instructions this worktree's tree contains only T-000..T-007 + T-008, and the full gate is green here (see Gate below) — the ruff/pyproject drift the Handoff describes belongs to concurrent T-006/T-007 work not present in this commit's tree.

## Gate
- `uv run pytest tests/test_app.py -q` → 8 passed.
- `uv run ruff check app tests/test_app.py && uv run ruff format --check app` → all checks passed / 4 files already formatted.
- `uv run python scripts/check.py` (full gate) → all checks passed: format (51 files), ruff, ruff format --check, mypy (default + `--platform linux`), pytest (172 passed, 97.08% coverage, ≥80% required), secrets scan clean.
