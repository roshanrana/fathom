# Security review T-020 — FINDINGS
Boundary: B1, B6  Risk class: medium

1. [HIGH] Free-text ticker input from every B1 surface (page `st.text_input` in `app/main.py`,
   `fathom fetch`/`quote`/`filings`/`brief`/`ask` in `fathom/cli.py`, the API path/query
   `ticker` in `fathom/api.py`, and the MCP tool `ticker` args in `fathom/mcp_server.py`) reaches
   `fathom/live/build.py::materialize()` — `symbol = ticker.upper()` then
   `live_dir = settings.live_cache_dir / symbol`, `manifest_path = live_dir / "manifest.json"`
   — with no call to `require_ticker`/`_LIVE_TICKER_RE` (`^[A-Z][A-Z0-9.\-]{0,9}$`) anywhere in
   `fathom/live/__init__.py::data_dir_for` or `build.py` before that path is built. A ticker like
   `..` or `../x` therefore reaches `Path(live_cache_dir) / ticker` unvalidated:
   `_read_fresh_manifest()` unconditionally does `manifest_path.exists()` + JSON-parses whatever
   is there for *any* traversed path before any SEC lookup runs, and (once `sec.lookup(symbol)`
   succeeds) the same unvalidated `symbol` is reused for `live_dir.mkdir(parents=True,
   exist_ok=True)` and the four `.to_parquet()` writes. `SecClient.lookup`'s exact-membership
   check against SEC's ticker map happens to gate the write side today, but that is incidental
   protection, not the validation this task was supposed to add — `05-m4-live-data.md` §5
   explicitly specifies `require_ticker(ticker, settings)` for this purpose, and T-019's review
   (`docs/tasks/T-019.security.md` finding 3) flagged that `PriceClient`/`facts.snapshot` rely on
   "an as-yet-unwritten caller" (this task) to validate first — `build.py` still doesn't. Every
   call site that reaches `require_ticker` (`briefing.py:159`, `data.py:74`, `filings.py:367`,
   `quotes.py:144`) calls it as `require_ticker(ticker)` with no `settings` argument, so even
   downstream the live-mode regex path is never exercised — control C-09 — fix required before
   merge: validate the upper-cased ticker against `_LIVE_TICKER_RE` in `materialize()` (or in
   `data_dir_for`) before any path is constructed, on every surface.

2. [MEDIUM] `fathom/api.py`'s `_STATUS_BY_CODE` (unchanged by this diff) maps only
   `UNKNOWN_TICKER`/`PROVIDER_CONFIG`/`PROVIDER_HTTP`/`PROVIDER_TIMEOUT`/`CONTRACT_INVALID`. The
   M4 codes `Code.SOURCE_CONFIG`/`SOURCE_HTTP`/`SOURCE_EMPTY` (raised by `materialize()` on every
   `?source=live` request path) are not in the map, so `_fathom_error_handler` falls through to
   the generic 500 `INTERNAL` body instead of the 502/503 `05-m4-live-data.md` §5 calls for. No
   internals actually leak (the 500 body is the same generic `{"code":"INTERNAL", ...}` used for
   unmapped/unexpected errors, satisfying C-12's "generic 500" rule) but the specific
   `SOURCE_CONFIG`/`SOURCE_HTTP`/`SOURCE_EMPTY` signal is lost to every API caller (page/API
   client), which is the surface-parity gap the design calls out — control C-12 — fix required
   before merge: add the three `SOURCE_*` codes to `_STATUS_BY_CODE` (502 for `SOURCE_HTTP`, 503
   for `SOURCE_CONFIG`/`SOURCE_EMPTY` per the task's own mapping).

Checks confirmed clear (no finding): `quotes.py::_live_sources` tolerates a missing or malformed
`manifest.json` (`OSError`/`ValueError` both caught, falls back to the fixture source constants);
API `source` query param is `Literal["fixture","live"] | None` via `Query()` — an invalid value
422s through FastAPI/Pydantic and only ever toggles between the two fixed settings-derived
directories, never an arbitrary path; `sec_contact` is read only from `settings.sec_contact` and
used solely to build the in-memory `User-Agent` string — absent from `LiveManifest`,
`manifest.json`, any `FathomError`, log call, API envelope, or the page; `unsafe_allow_html` does
not appear anywhere in `app/`; no `pyproject.toml`/`uv.lock` change in this commit (`git show
--stat HEAD`); `fathom fetch` prints ticker/cik/fetched_at, form/date/accession + section *ids*,
bars range/source, and snapshot field *names* only — never `filing.text` or section bodies.

Scanner summary: secrets 0, ruff all checks passed, deps unchanged: yes

# Security review T-020 — FINDINGS (attempt 2)
Boundary: B1, B6  Risk class: medium
Closure: F1 OPEN — `materialize()` (`fathom/live/build.py:130`) now calls `require_ticker(ticker,
settings)` before `live_dir = settings.live_cache_dir / symbol` is built, and every surface
(`app/main.py:64`, `fathom/cli.py:73/87/99/220`, `fathom/mcp_server.py:69/80`, `fathom/api.py` via
`data_dir_for`, `fathom/ask.py:281`, `fathom/briefing.py:280`, plus `require_ticker(ticker,
data_dir=...)` in `data.py`/`quotes.py`/`filings.py`/`briefing.py`) now routes through
`data_dir_for`/`materialize`/`require_ticker` before touching a path — 9 of the 10 required probe
inputs (`../x`, `..\x`, `A/B`, `A\B`, `A%2F`, ``, `"A"*11`, a unicode look-alike, a NUL byte) are
rejected by `_LIVE_TICKER_RE` before any filesystem/HTTP call, and the resulting path for a valid
ticker (`AAPL`) resolves strictly inside `live_cache_dir` (`is_relative_to` verified true). But
`_LIVE_TICKER_RE.match()` (`fathom/data.py:59`, not `.fullmatch()`) lets `"AAPL\n"` through — `$`
matches just before one trailing `"\n"` — confirmed by direct regex test (match=True,
fullmatch=False). That symbol then reaches `live_dir = live_cache_dir / "AAPL\n"` and
`manifest_path.exists()` unrejected, before SEC's incidental membership check. This is the exact
bug class this same diff explicitly fixed with `.fullmatch()` in `fathom/live/facts.py:68`
(`_CIK_RE`) and `fathom/live/prices.py:77/103` (`_TICKER_RE`), each commented "T-023 attempt 2
F2... Python's `$` matches end-of-string *or* just before one trailing `\n`" — but the same fix was
not applied to `_LIVE_TICKER_RE`, the regex `require_ticker`/`materialize` actually enforce for the
path-construction boundary F1 covers. F2 CLOSED — `fathom/api.py:39-41` adds `SOURCE_CONFIG:503,
SOURCE_HTTP:502, SOURCE_EMPTY:404` to `_STATUS_BY_CODE`, matching `docs/design/05-m4-live-data.md:130`
exactly (finding 2's own suggested fix text said "503 for SOURCE_EMPTY", but the design doc — the
authoritative source — specifies 404, and the implementation correctly follows the design doc, not
the finding's paraphrase); error bodies still carry only `{code, message}`, never provider bodies.
1. [MEDIUM] `_LIVE_TICKER_RE` in `fathom/data.py:19,59` uses `.match()` not `.fullmatch()`, so a
   ticker ending in one `"\n"` (e.g. `"AAPL\n"`) passes `require_ticker` in every mode and reaches
   `live_dir = settings.live_cache_dir / symbol` and `manifest_path.exists()`
   (`fathom/live/build.py:130-132`) before any further validation — control C-09 — fix: change
   `_LIVE_TICKER_RE.match(upper)` to `_LIVE_TICKER_RE.fullmatch(upper)`, mirroring the sibling fix
   already applied to `_CIK_RE` and `_TICKER_RE` in this same commit.
2. [LOW] `fathom/quotes.py::_live_sources` (lines 20-39, unchanged by this diff) parses
   `manifest.json` with bare `json.loads` + `.get()`; valid-JSON-but-non-dict content (a JSON
   string/number/array/null) raises an uncaught `AttributeError` instead of falling back the way
   the caught `OSError`/`ValueError` cases do — confirmed by direct test. Not attacker-reachable
   (`manifest.json` is written only by `materialize()`) and not introduced by this diff, but the
   "manifest.json reading tolerates malformed content" probe does not fully hold here — control
   C-09 — no merge block; consider also catching `AttributeError` or `isinstance`-guarding after
   `json.loads`.
Scanner summary: secrets 0, ruff all checks passed, deps unchanged: yes
