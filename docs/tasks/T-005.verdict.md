# Verdict T-005 — PASS
Reviewed: f7420e9becc42a6b61caf4e9ef5fb076826e7709  Tier: T2  Attempt: 1

## Acceptance criteria
- AC1 (contract round-trips / extra-key ignore / missing-list failure): PASS. `Briefing`,
  `Answer`, `BriefingDraft`, `AnswerDraft` round-trip through `model_dump_json` /
  `model_validate_json` (re-verified with independent fixture data, not just the implementer's
  fixtures). `BriefingDraft.model_validate({...six lists..., "extra_key_xyz": 1})` ignores the
  extra key (all six lists all `is_required() == True`); a draft missing `talking_points` raises
  `ValidationError`, confirmed independently.
- AC2 (advice patterns, 14/14, adversarial + benign): PASS. Programmatically diffed all 14
  `ADVICE_PATTERNS` pattern strings, in order, against the LLD §6.4 text — byte-for-byte
  identical, all compiled with `re.IGNORECASE`. All 11 AC2-named adversarial phrases →
  `is_advice() == True`; the 5 LLD-frozen benign phrases + AC2's 7 → `False`. Independently wrote
  11 more adversarial phrases and 5 more benign phrases (not in the implementer's test file): 10/11
  adversarial passed; one, "Does the stock price rally after earnings?", did not match pattern 11
  (`\b(will|is|does)\s+(the\s+)?(stock|share price|price|it)\s+(go|...|rally|...)\b`) because that
  alternation has no combined "stock price" phrase — this is a gap in the *frozen* LLD regex text
  itself (verified identical to spec), not a transcription defect, and out of this task's scope to
  alter. All 5 independent benign phrases → `False` (no false positives).
- AC3 (verify_claim): PASS. Independently re-ran: curly quotes + extra whitespace vs straight
  quotes/newlines → `True`; 5-word quote → `False`; 61-word quote → `False`; one-word-changed
  quote → `False`; boundary case of exactly 6 words → `True` (sanity check, not a blocker).
- AC4 (scrub_claims): PASS. Independently built two claims; flagged claim gets
  `text == GUARD_NOTICE`, `guarded=True`, `verified=False`, `source` preserved; unflagged claim
  returned unchanged (`==` and same object); hit count correct; original input list's element
  identity and value unchanged after the call (no mutation).
- AC5 (record): PASS. Independently exercised with a fresh temp dir: parent directories created;
  one JSON line per call; with `audit_bodies=False` the `prompt`/`response` keys are dropped
  entirely (not merely nulled) even when the record carried body text — no leakage into the file;
  with `audit_bodies=True` both are kept verbatim; pointing `audit_path` under a path whose parent
  segment is a regular file raises `FathomError(Code.AUDIT_WRITE)` with the target path in both
  the message and `details["path"]`.
- AC6 (never-log): PASS. Attached a capturing `logging.Handler` to logger `"fathom"`, called
  `record` (with independent fake secret tokens distinct from the implementer's test fixtures) and
  `is_advice`/`verify_claim`/`scrub_claims`; the only emitted record is
  `"audit written path=<path>"` — no occurrence of `"sk-ant-"`, `"PORTKEY"`, the prompt body, or
  the question text in any captured message.
- AC7 (mypy strict / test naming): PASS. `uv run mypy fathom` (strict = true in `pyproject.toml`)
  → "Success: no issues found in 12 source files". All 20 new test functions across the three
  files use only `test_fr007_*`, `test_fr008_*`, `test_fr011_*`, `test_nfr006_*`, `test_nfr008_*`
  prefixes — matches AC7 exactly, no stray prefixes.

## Verification checklist
- [x] scope respected — `git show --name-only HEAD` touches only `fathom/audit.py`,
      `fathom/contracts.py`, `fathom/guard.py`, `tests/test_audit.py`, `tests/test_contracts.py`,
      `tests/test_guard.py`, and the pack file itself (Handoff section).
- [x] `AUDIT_WRITE` used — `fathom/audit.py` raises `FathomError(Code.AUDIT_WRITE, ...)` on
      `OSError` from the mkdir/write, naming only the path.
- [x] never-log honoured — confirmed above (AC6); success path logs only the destination path.
- [x] tests named with RTM IDs — confirmed above (AC7).
- [x] no new dependencies — `pyproject.toml` / `uv.lock` untouched by this commit; only
      `pydantic`, stdlib (`re`, `json`, `logging`, `datetime`, `typing`) imported by the three
      new modules.
- [x] patterns match LLD §6.4 exactly, count = 14 — confirmed programmatically (AC2).

## Findings
No blocking findings.
- [LOW, informational] The frozen LLD §6.4 pattern 11 does not catch "stock price <verb>"
  phrasing (only "share price" is a two-word alternative); this is inherited from the frozen spec
  text, reproduced verbatim by the implementation, and not something T-005 was scoped to fix.
  Worth a note for a future LLD revision, not a blocker here.
- Pre-existing security review `docs/tasks/T-005.security.md` is CLEAR with two LOW,
  non-blocking notes (guard scans `Claim.text` only, not `.quote`; no homoglyph normalisation) —
  both explicitly flagged as accepted residual risk matching the threat model, not regressions
  introduced by this diff.

## Gate
`uv run python scripts/check.py`: all checks passed (ruff check, ruff format --check, mypy fathom,
mypy fathom --platform linux, pytest, secrets scan). Full suite: 147 passed, 0 failed. Coverage:
97.50% overall; `fathom/contracts.py`, `fathom/guard.py`, `fathom/audit.py` each 100%. Targeted
`uv run pytest tests/test_contracts.py tests/test_guard.py tests/test_audit.py -q`: 63 passed.
