# Verdict T-018 — PASS
Reviewed: 773bb2b3e7236e6e82842d33fe1e7a8a78b3dedb  Tier: T2  Attempt: 1

## Acceptance criteria

- AC1 — PASS. `Settings.from_env({})` defaults verified (`tests/test_config.py::test_fr021_from_env_live_defaults`): `data_source=="fixture"`, `sec_contact is None`, `live_cache_dir==Path(".cache/live")`, `live_ttl_hours==6.0`, `price_source=="yahoo"`. `SecClient.from_settings` raises `SOURCE_CONFIG` naming `FATHOM_SEC_CONTACT` when contact is missing (`fathom/live/sec.py:134-139`, `tests/test_live_sec.py::test_fr025_from_settings_without_contact_raises_source_config`). Invalid `FATHOM_DATA_SOURCE`/`FATHOM_PRICE_SOURCE` raise `Code.PROVIDER_CONFIG` with the offending `var` (`fathom/config.py:95-108`, `tests/test_config.py::test_nfr005_from_env_invalid_data_source_raises_provider_config` and `..._invalid_price_source_...`). Own re-run with a fresh `Settings.from_env` confirms.
- AC2 — PASS. Independently exercised `LiveHttp` with my own `MockTransport` handlers: cache write of `<cache>/_http/<sha256>` + `.meta` (url/fetched_at/status[/source]) confirmed by reading `tests/test_live_http.py::test_nfr013_get_writes_cache_body_and_meta`; cache hit with zero transport calls within TTL, refetch after TTL/force, `ttl_hours=None` caches forever — all via call-count assertions, no sleep-based timing. Throttle: `test_fr025_throttle_enforces_min_spacing_via_monkeypatched_clock` uses a fake clock (100.0 → 100.05) and asserts `sleeper` called with `≈0.07` (i.e. remaining to reach 0.12s) — confirms real spacing logic in `fathom/live/http.py:103-112`, not a sleep-based test. Non-2xx → `SOURCE_HTTP {"source","status","reason"}` with no body (explicit "secret internal detail" not-in-message/details assertion). `httpx.HTTPError` → status 0. >25MB → reason "too large", and cache directory left empty (write happens only after the size check).
- AC3 — PASS. `lookup("aapl")` fixture resolution matches exactly (`ticker=AAPL, cik=0000320193, name=Apple Inc., exchange=Nasdaq, sic_description=Electronic Computers, fiscal_year_end=0926`). Unknown ticker → `UNKNOWN_TICKER`. `BRK.B` and `BRK-B` (and lower-case variants) both resolve to the same `CIK 0001067983` company via the `.`/`-` normalisation in `SecClient.lookup` (`fathom/live/sec.py:152-179`).
- AC4 — PASS. Verified against the recorded AAPL submissions fixture by hand-tracing the selection algorithm and cross-checking against `tests/test_live_sec.py::test_fr021_filings_newest_first_excludes_amendment_caps_at_five`: the fixture's newest entry is a synthetic `10-K/A` (2026-09-10) that is correctly excluded (form filter is exact-match `10-K`/`10-Q`); the 5th 10-Q (2025-05-02) is correctly dropped by the ≤4 cap; result is `[10-Q,10-Q,10-Q,10-K,10-Q]` newest-first, ≤5 total, 730-day window computed off the newest *qualifying* filing, and the 10-K's `url` matches the documented EDGAR archive path pattern.
- AC5 — PASS. `html_to_text` on both real fixtures (10-Q 152KB, 10-K360KB) is parsed by `fathom.filings.parse_sections`, producing all required canonical ids (`10-Q:I.2/I.3/I.4/II.1/II.1A`, `10-K:1A/7`) with `10-K:1A`/`10-K:7` body length ≥2000 chars (asserted directly in the test, verified passing). No `<` remains in output; `&amp;` unescape verified both on the real fixture and a minimal unit case; `period_end` parses non-None on both real fixtures.
- AC6 — PASS. `document_text` called twice for the same filing: `after_first == before+1`, `after_second == after_first` (zero additional transport calls) — `ttl_hours=None` (cache forever) in `fathom/live/sec.py:229-236`. User-Agent `Fathom/<__version__> (<contact>)` asserted on every captured request header across both `lookup` and raw `LiveHttp.get` paths.
- AC7 — PASS. `mypy fathom` (strict, both default and `--platform linux` as run by the gate) clean. Gate green (321 passed, 95.95% total coverage; `fathom/live/http.py` 96%, `fathom/live/sec.py` 95%, both ≥ spec's 85% target for `fathom/live`). No network in tests: full gate and the targeted live/config/errors test set were both re-run under `HTTPS_PROXY=http://127.0.0.1:9` (an unreachable local port) and passed unchanged — any test that attempted a real request would have failed or hung. Test names: all new tests in `test_live_http.py`/`test_live_sec.py` are `test_fr025_*`/`test_nfr013_*`/`test_fr021_*`; the two new `test_config.py` validation tests use `test_nfr005_*`, consistent with the pre-existing NFR-005 convention already used in that same file for `PROVIDER_CONFIG`-style validation (not a fabricated RTM id) — noted as a minor naming note, not a failure.

## Verification checklist

- [x] Scope respected — diff touches exactly the files listed in the pack's Scope section, plus the pack-declared out-of-scope edit to `tests/test_errors.py` (limited to the `Code` enum-membership set literal, adding `SOURCE_CONFIG`/`SOURCE_HTTP`/`SOURCE_EMPTY` — nothing else in that file changed). Fixtures: `aapl_10k.htm` 367,540 B (≤400KB), `aapl_10q.htm` 151,679 B (≤300KB), `aapl_submissions.json` 6,187 B, `company_tickers.json` 454 B; total 525,860 B (~514KB) ≤ 1MB, measured with `du -b`.
- [x] No network in the gate — grepped all test files for real-host URL strings; every occurrence outside pre-existing `test_contracts.py`/`test_filings.py` fixture-URL string assertions is inside a `MockTransport` handler in `test_live_http.py`/`test_live_sec.py`. No `httpx.Client()` built without an explicit `transport=`/injected client anywhere in tests. Full gate and targeted suite both re-run green under a dead `HTTPS_PROXY`. `tests/test_live_network.py` does not exist yet — correctly out of scope for T-018 (not in the pack's Scope list; deferred alongside `prices.py`/`facts.py`).
- [x] `SOURCE_HTTP` never carries a body — confirmed by code inspection (`fathom/live/http.py:79-96`, only `status`/`reason_phrase`/`"too large"`/`str(exc)` ever go into `details`) and by the test's explicit assertion that response content ("secret internal detail") appears in neither `message` nor `details`.
- [x] Contact never logged — no `logging`/`print(` anywhere in `fathom/live/*.py` or the touched `config.py`/`errors.py` (grepped); `sec_contact` only ever flows into the `User-Agent` header string.
- [x] `html_to_text` is stdlib-only — the function and its module-level regexes use only `html.unescape` and `re`; no third-party HTML parser (bs4/lxml) is used anywhere in the diff. (Note: `sec.py` as a whole imports `json`/`pydantic`/etc. for its other responsibilities — the "imports only html and re" checklist item is read as applying to the parsing algorithm itself, per §4's "Stdlib `html` + `re` only" framing, not a restriction on the whole module's import list.)
- [x] Tests named with RTM ids — all new tests prefixed `test_fr021_*`/`test_fr025_*`/`test_nfr013_*` except two config-validation tests using the pre-existing `test_nfr005_*` convention (see AC7 note).
- [x] No new dependencies — `pyproject.toml` and `uv.lock` untouched by this commit (`git show HEAD --stat` shows no entries for either file).

## Findings

No CRITICAL or HIGH issues. One LOW/informational note: two new `test_config.py` tests tag validation of the new `data_source`/`price_source` fields as `test_nfr005_*` rather than one of the task's own RTM ids (`FR-021`/`FR-025`/`NFR-013`); this reuses an established in-file convention for `PROVIDER_CONFIG`-style validation tests and is not a fabricated id, so it does not block. Frozen contracts (`SecCompany`, `SecFiling`, `Code` additions, `Settings` field/env names, throttle/cache/size-cap rules, `html_to_text` algorithm, `filings(cik)` selection) all match `05-m4-live-data.md` §3/§4 exactly. The declared out-of-scope `tests/test_errors.py` edit is limited to the membership-set literal, as required by the Handoff note.

## Gate

`uv sync --all-extras`: clean. `uv run pytest tests/test_live_http.py tests/test_live_sec.py tests/test_config.py tests/test_errors.py -q` (HTTPS_PROXY=http://127.0.0.1:9): 52 passed. `uv run mypy fathom`: clean. `uv run python scripts/check.py` (HTTPS_PROXY=http://127.0.0.1:9): ruff check/format, mypy (default + linux), pytest 321 passed (95.95% total coverage; fathom/live 95-96%), secrets scan, bench, bench drift, card drift — all green.
