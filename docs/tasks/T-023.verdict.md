# Verdict T-023 — PASS
Reviewed: b58134f477e88eee1cb9c56586a5b00428267e03  Tier: T2  Attempt: 1

## Acceptance criteria

- **AC1 — PASS.** Independently reproduced all four malformed-Yahoo-body cases with a
  fresh `httpx.MockTransport` (null `timestamp` entry, string OHLCV values, mismatched
  array lengths that leave zero valid bars, `{"chart":{"result":null}}`): each requests
  Stooq and yields `last_source == "stooq"`. When Stooq also fails (garbage CSV) for each
  of the four, the result is `SOURCE_HTTP` with `details == {"source": "stooq", "status":
  200, "reason": "malformed response"}` — `details.keys()` is exactly `{source, status,
  reason}` and the injected marker string never appears in `details` or `message`. Note:
  a *partial* length mismatch that still yields ≥1 valid bar correctly does NOT fall back
  (matches the frozen rule in the pack: "Yahoo failure" = cannot parse into ≥1 valid bar) —
  confirmed this is intentional, not a gap.
- **AC2 — PASS.** A 20,000-row Stooq CSV yields ≤10,000 bars (`pd.read_csv(..., nrows=10_000)`
  in `fathom/live/prices.py:142`). A single non-numeric cell drops only that row (2 of 3 rows
  survive). An all-garbage CSV raises `SOURCE_HTTP`.
- **AC3 — PASS.** `daily_bars("bad ticker!")` raises `UNKNOWN_TICKER` with zero HTTP calls
  (asserted via a transport that raises if invoked). `snapshot(sec, "12", ...)` raises
  `SOURCE_HTTP` with `reason == "invalid identifier"` and zero HTTP calls.
- **AC4 — PASS.** `NaN` shares -> `market_cap` `None`; `Infinity` equity -> `pb` `None`;
  out-of-bounds EPS (1e6 > 1e4 bound) -> `pe` `None`; out-of-bounds DPS (`-inf`) -> treated
  as missing, `dividend_yield` defaults to `0.0` per the frozen DPS-absent rule.
  `json.dumps(result, allow_nan=False)` succeeds.
- **AC5 — PASS.** Full gate (`uv run python scripts/check.py`) green: ruff check/format,
  mypy strict (both native and `--platform linux`), 357/357 tests, secrets scan, bench/card
  drift all pass. `uv run mypy fathom` clean on its own. All new/changed tests in
  `tests/test_live_prices.py` / `tests/test_live_facts.py` are named `test_fr022_*` /
  `test_fr023_*`.

Public signatures unchanged vs `2ea30ba`: `git diff 2ea30ba HEAD -- fathom/live/prices.py
fathom/live/facts.py` shows only private (`_`-prefixed) helpers added/changed
(`_within_*_bounds`, `_finite_or_none`, `_malformed_response_error`, `_valid_close`,
`_parse_stooq_row`, `_required_float`, and internal params added to `_numeric_val` /
`_latest_value` / `_ttm_sum`). `PriceClient.__init__`, `PriceClient.daily_bars`, and
`snapshot(...)` signatures are byte-identical to `2ea30ba`.

## Findings

All five T-019 findings closed, independently confirmed against the diff (not just re-run
of the implementer's own tests):

- **F1 (HIGH)** — closed. `_yahoo()` now runs `json.loads` + `_bars_from_yahoo` inside a
  `try/except (TypeError, ValueError, KeyError, IndexError, json.JSONDecodeError)` guard
  (`_PARSE_GUARD_EXCEPTIONS`); `_valid_close`/`_safe_float` additionally guard the per-cell
  conversions. 500-iteration fuzz (random bytes as both Yahoo and Stooq bodies, incl. `NaN`/
  `Infinity`/`null` tokens) against `PriceClient.daily_bars` produced zero uncaught
  exceptions — only `FathomError` or a clean return.
- **F2 (HIGH)** — closed. `pd.read_csv(..., nrows=10_000)` caps row count; `_parse_stooq_row`
  wraps each row's parse in the same guard tuple and returns `None` (row dropped) rather than
  propagating.
- **F3 (MEDIUM)** — closed. `_TICKER_RE` (`^[A-Z0-9.\-]{1,10}$`) checked in `daily_bars`
  before any fetcher call; `_CIK_RE` (`^\d{10}$`) checked in `snapshot` before any concept
  fetch. Both verified zero-request in isolation.
- **F4 (MEDIUM)** — closed. `_numeric_val` checks `math.isfinite`; `_finite_or_none` is a
  final belt-and-suspenders check on `market_cap`/`pe`/`pb`/`dividend_yield`.
- **F5 (LOW)** — closed. Per-concept bounds (`_within_shares_bounds`, `_within_equity_bounds`,
  `_within_eps_bounds`, `_within_dps_bounds`) threaded into `_latest_value`/`_ttm_sum` via the
  `is_valid` callback; out-of-range values are treated as missing (same path as absent data).

No new findings. Scope respected: diff touches exactly `fathom/live/prices.py`,
`fathom/live/facts.py`, `tests/test_live_prices.py`, `tests/test_live_facts.py`, and the
task pack — no other file changed (`git show --stat HEAD`). No body/URL ever appears in a
`FathomError` — checked across all AC1 both-fail variants and the existing size/status
error paths in `fathom/live/http.py` (untouched, previously cleared).

## Gate

`uv run python scripts/check.py` — **all checks passed** (ruff check, ruff format --check,
mypy strict x2, pytest 357 passed / 95.82% coverage, secrets scan, bench, bench drift, card
drift). `uv run mypy fathom` — clean. `uv run pytest tests/test_live_prices.py
tests/test_live_facts.py -q` — 30 passed. As expected for this commit's tree (T-018/T-019/
T-022/T-023 only, T-020 wiring not yet committed), the full gate is green with no
pre-existing blockers.

# Verdict T-023 — PASS (attempt 2)
Reviewed: 2623509cb48cd56be1c60f072ab462b6d57e5a0a  Tier: T2  Attempt: 2

## Acceptance criteria

- **AC1/security-F1 — PASS.** `_yahoo`/`_bars_from_yahoo` (fathom/live/prices.py:98-150) now
  validate shape at every level: `isinstance(result, dict)` (line 107) before use,
  `isinstance(indicators, dict)` (line 119), `isinstance(quote_list, list)` (line 122),
  `isinstance(quote, dict)` (line 125) — each raises `TypeError` on failure, and
  `_PARSE_GUARD_EXCEPTIONS` now includes `AttributeError` (line 44) as defense in depth. Both
  remediation options the security doc offered were applied together.
- **AC2/security-F2 — PASS.** `_TICKER_RE.fullmatch` (prices.py:79) and `_CIK_RE.fullmatch`
  (facts.py:70) replace `.match` + `$`-anchored patterns, closing the trailing-`\n` bypass.
- **AC3 (regression) — PASS.** Full gate green, all prior T-019/T-023 findings (F1-F5 from
  attempt 1, now closed a second time for the two reopened HIGHs) remain closed.
- **Signatures — PASS.** `git diff 2ea30ba HEAD -- fathom/live/prices.py fathom/live/facts.py`
  shows `PriceClient.__init__`, `PriceClient.daily_bars`, and `snapshot(...)` byte-identical
  to `2ea30ba`; only private helpers (`_within_*_bounds`, `_finite_or_none`,
  `_malformed_response_error`, `_valid_close`, `_parse_stooq_row`, `_required_float`, and the
  new `isinstance` guards) changed. Diff since attempt 1 (`dbe6ffa..2623509`) touches exactly
  `fathom/live/prices.py`, `fathom/live/facts.py`, `tests/test_live_{prices,facts}.py`, and the
  task doc — no `pyproject.toml`/`uv.lock` change.

## Probes

Independent `httpx.MockTransport` harness (own script, not the implementer's tests):

- Five malformed-Yahoo-shape bodies — `{"chart":{"result":[["x"]]}}`,
  `{"chart":{"result":[{"indicators":"x"}]}}`, `{"chart":{"result":[{"indicators":{"quote":"x"}}]}}`,
  `{"chart":{"result":[{"indicators":{"quote":[null]}}]}}`,
  `{"chart":{"result":[{"timestamp":"x","indicators":{"quote":[{}]}}]}}` — each: Stooq URL was
  requested (fallback attempted, confirming AttributeError no longer bypasses it) and, with
  Stooq primed to fail on zero valid rows, raised `FathomError(SOURCE_HTTP)` with
  `details.reason == "malformed response"`. No uncaught `AttributeError`/`TypeError` in any case.
- `daily_bars("AAPL\n")`, `daily_bars("AAPL\r")`, `daily_bars(" AAPL")` — each raised
  `UNKNOWN_TICKER` with zero transport calls.
- `snapshot(sec, "0000320193\n", 100.0, ...)` — raised `SOURCE_HTTP` with
  `details.reason == "invalid identifier"` and zero transport calls.
- 300-iteration random-bytes fuzz against `daily_bars("AAPL")`, run twice (random bytes as the
  Yahoo body with Stooq failing, and as the Stooq body with Yahoo failing — 600 total attempts):
  zero uncaught exceptions, only `FathomError` or a clean return.
- Signature check (see above) confirms no drift vs `2ea30ba`.

All probes passed (25/25 assertions; 0 failures) on first correctly-constructed attempt —
one earlier probe run used a Stooq fallback body that didn't start with the documented CSV
header, which legitimately produced `reason == "not a CSV body"` instead of `"malformed
response"`; that was a probe-construction issue, not a code defect, and was corrected before
drawing a conclusion.

## Findings

Both HIGHs from `docs/tasks/T-023.security.md` are closed and independently reconfirmed:

- **F1 (HIGH, non-dict Yahoo `result`/`indicators` → uncaught `AttributeError`)** — closed.
  `isinstance` guards at every unpacking step plus `AttributeError` added to
  `_PARSE_GUARD_EXCEPTIONS`. Verified the Stooq fallback is actually attempted (not skipped)
  for all five shapes named in the security doc and in the probe list above.
- **F2 (HIGH, `$`-anchored regex lets `"AAPL\n"`/`"0000320193\n"` through)** — closed.
  `fullmatch` used in both `_TICKER_RE` and `_CIK_RE`; verified `\n`, `\r`, and leading-space
  variants are all rejected pre-request.

No new findings. Scope respected — diff since attempt 1 touches only the two source files,
their tests, and the task doc.

## Gate

`uv sync --all-extras` — clean. `uv run pytest tests/test_live_prices.py tests/test_live_facts.py -q`
(with `HTTPS_PROXY=http://127.0.0.1:9`) — 35 passed. `uv run mypy fathom` — clean, no issues in
24 source files. `uv run python scripts/check.py` — **all checks passed**: ruff check, ruff
format --check, mypy strict (native + `--platform linux`), pytest 390 passed / 95.64% coverage
(≥80% required), secrets scan (0 found), bench, bench drift, card drift. Consistent with this
tree carrying T-020 attempt 1's wiring, the full gate is green with no pre-existing blockers.
