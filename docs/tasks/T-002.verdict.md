# Verdict T-002 — PASS
Reviewed: adeddc1ca880a23827ce954f7926709dbef3af9a  Tier: T2  Attempt: 1

## Acceptance criteria
- AC1: PASS — `filings_for("AAPL", data_dir)` returns 5 filings, newest first
  (`[2026-05-01, 2026-01-30, 2025-10-31, 2025-08-01, 2025-05-02]`); `filings[0]` is the
  10-Q filed 2026-05-01; the 10-K has `accession == "0000320193-25-000079"`,
  `edgar_url == "https://www.sec.gov/Archives/edgar/data/320193/000032019325000079/"`,
  `period_end == date(2025, 9, 27)`. Verified independently outside the test suite.
- AC2: PASS — synthetic TOC ("Item 1A. Risk Factors 5") followed by a real body: parser
  returns the body occurrence; `char_start` equals the index of the real header line, not
  the TOC line. Verified independently with a fresh synthetic string.
- AC3: PASS — synthetic 10-Q with PART I/II and Items 2, 4, 1, 1A yields exactly
  `['10-Q:I.2', '10-Q:I.4', '10-Q:II.1', '10-Q:II.1A']` in that order; Part II Item 1A
  ("None.") is kept with `text == "Item 1A. Risk Factors\nNone.\n"` — a short real body
  is not discarded. Verified independently.
- AC4: PASS — full 97-accession fixture loop (independently executed, not just the
  shipped test): 20 10-Ks, all 20 have both `10-K:1A` and `10-K:7`; 77 10-Qs, all 77 have
  `10-Q:I.2`. Zero parse failures. `AC4_ALLOWLIST` in tests/test_filings.py is `{}` —
  empty, and correctly so (no fixture gaps exist).
- AC5: PASS — `sections_for(accession, data_dir)` called twice returns the identical
  object (`first is second`, functools.cache). For every section of the AAPL 10-K,
  `Section.text == normalised_text[char_start:char_end]` where
  `normalised = raw_text.replace("\xa0", " ")`. Verified independently.
- AC6: PASS — `period_end` on `"For the fiscal year ended\nSeptember\xa027\n, 2025"` →
  `date(2025, 9, 27)`; on `"For the quarterly period ended March 29, 2025"` →
  `date(2025, 3, 29)`; on a string with no period-end phrase → `None`. Verified
  independently.
- AC7: PASS — `uv run mypy fathom` → "Success: no issues found in 6 source files" (both
  default and, via the full gate, `--platform linux`). All tests in
  tests/test_filings.py are named `test_fr004_*` or `test_fr005_*` (11 tests, matching
  the pack's RTM ids FR-004/FR-005).

## Verification checklist
- [x] scope respected — commit touches only `docs/tasks/T-002-filings-parser.md`,
      `fathom/filings.py` (new), `tests/test_filings.py` (new).
- [x] error taxonomy used — `Code.PARSE_FAILED` raised on zero canonical sections;
      `Code.DATA_MISSING` / `Code.UNKNOWN_TICKER` used appropriately elsewhere in the
      module, all pre-existing `Code` members (no new codes invented).
- [x] no filing text logged — no `print`/`logging`/`logger` calls in `fathom/filings.py`;
      `FathomError` messages reference only form/accession/ticker identifiers, never
      filing text.
- [x] tests named with RTM IDs — all 11 tests prefixed `test_fr004_` or `test_fr005_`.
- [x] no new dependencies — no changes to `pyproject.toml` or `uv.lock` in this commit.
- [x] AC4 allowlist empty or justified — empty (`{}`), and independently confirmed
      correct: 0/97 fixtures fail the coverage requirement.

## Spec conformance (LLD §2.5, literal check)
- `HEADER`, `PART`, and `PERIOD_END` regexes in `fathom/filings.py` are byte-for-byte
  identical to the frozen patterns in `docs/design/03-lld.md` §2.5.
- Body = end of header line → start of next header (or end of text); grouping by
  `(part, item.upper())`; longest-body-wins selection; canonical-ids-only emission in
  document order (sorted by the surviving occurrence's `char_start`); zero canonical
  sections → `PARSE_FAILED` — all match steps 1–6 literally.
- `CANONICAL_SECTIONS` has exactly 12 entries, in the LLD table's order, ids and titles
  matching exactly.
- `edgar_url` matches the spec's format string exactly (`int(cik)`, dashes stripped from
  accession).

## Quality spot-checks (non-blocking)
- AAPL 10-K `10-K:1A` first 200 chars: `"Item 1A.    Risk Factors\nThe following
  summarizes factors that could have a material adverse effect..."` — genuine body text,
  not a TOC line.
- AAPL 10-K `10-K:7` first 200 chars: `"Item 7.    Management's Discussion and Analysis
  of Financial Condition and Results of Operations\nThe following discussion should be
  read in conjunction..."` — genuine body text, not a TOC line.

## Findings
None blocking. No non-blocking findings either — implementation is a literal, clean
match to the frozen algorithm and all fixtures pass coverage with an empty allowlist.

## Gate
`uv run python scripts/check.py` → all checks passed (ruff check, ruff format --check,
mypy fathom, mypy fathom --platform linux, pytest, secrets scan).
Full suite: 45 passed (11 in tests/test_filings.py). Total coverage: 97.32% (required
80%); `fathom/filings.py` itself: 95% (110 stmts, 5 missed: lines 104-105, 176, 192, 204
— unreachable/defensive branches: `period_end` ValueError guard, `sections_for`
DATA_MISSING branch, `filings_for` DATA_MISSING and unexpected-form branches).
