# Verdict T-024 — FAIL
Reviewed: 86a6434cc55775cb08d5c19d7c14cbb3ffde3045  Tier: T2  Attempt: 1

## Acceptance criteria
- AC1 (malformed bodies -> SOURCE_HTTP "malformed response", no body/URL leak): PASS. Own
  independent probe of the 8 specified bodies (`\xff\xfe`, `not json`, `[]`, `null`, `"s"`, `7`,
  `""`, 3 MB of `[`) against all four call sites — ticker map, submissions, an older-filings
  page, and `company_concept` — is 32/32 clean: every case raises `FathomError(SOURCE_HTTP,
  reason="malformed response")` with `status: 200`, and neither `err.message` nor `err.details`
  contains any byte of the body or the request URL. Valid fixtures (`aapl_submissions.json`,
  `company_tickers.json`) still decode and produce correct `SecCompany`/`SecFiling` results.
- AC2 (1000-iteration mutation fuzz raises only `FathomError`): **FAIL**. The suite's own seeded
  test (`test_fr021_fuzz_mutated_submissions_and_ticker_map_raise_only_fathom_error`, seed
  `20240024`, 200 iterations) fails deterministically:
  `fathom/live/sec.py:227: KeyError: 'cik_str'` escaping `sec.lookup("AAPL")` uncaught. A
  1-to-8-byte random mutation of `company_tickers.json` can flip bytes inside a string value
  without breaking JSON syntax, so `_decode_json` correctly returns a `dict` (it only checks
  "parses as JSON" + "top-level is a dict") but an individual ticker entry then lacks the
  `"cik_str"`/`"ticker"` key it had before mutation, and `entry["cik_str"]` /
  `entry["ticker"]` index directly into it. Independently reproduced with a different seed
  (999024, 1000 iterations, this review's own harness): `lookup()` 1/1000 uncaught
  `KeyError: 'ticker'`, `filings()` 1/1000 uncaught `KeyError: 'primaryDocument'`. See Findings.
- AC3 (signatures unchanged; full gate green; mypy strict; docs figure == gate): **FAIL** on the
  gate-green and docs-match sub-clauses. `uv run python scripts/check.py` stops at the `pytest`
  step: `1 failed, 455 passed, 2 skipped` (the AC2 test above), exit 1 — it never reaches the
  secrets scan, bench, bench-drift or card-drift steps, so "all checks passed" is never printed.
  `docs/SHOWCASE.md` §1 was updated in this commit to read **"456 passed (2 network tests
  skipped offline), 95.62% coverage, all checks passed"** — that figure does not match the
  actual gate output and the gate is not green, so the quoted claim is false as committed.
  Signatures and mypy are fine on their own: `ticker_map`, `lookup`, `filings`, `document_text`,
  `company_concept` are byte-identical to `773bb2b`; `_submissions` (private) kept its signature;
  `uv run mypy fathom` (host) is clean, and `mypy fathom --platform linux` passed as part of the
  gate run before it stopped at pytest.
- Verification checklist: "no unguarded `json.loads` remains in fathom/live" — confirmed,
  `grep -rn "json.loads" fathom/live` returns exactly one hit, `fathom/live/sec.py:119`, inside
  the guarded `_decode_json` helper (`prices.py:99` and `http.py:131` are pre-existing, separately
  guarded/out-of-scope call sites, unchanged by this diff). "no body in errors" — confirmed
  clean across all 32 AC1 probes. "docs figures equal the gate" — **not met**, see AC3.

## Fuzz summary
- AC1 malformed-body probe (8 bodies x 4 call sites = 32 cases): 32/32 clean `SOURCE_HTTP`
  "malformed response", 0 body/URL leaks.
- Suite's seeded AC2 test (seed 20240024, 200 iterations): **fails** on the first mutated
  ticker-map iteration that corrupts a per-entry key rather than the JSON syntax.
- Independent review fuzz (seed 999024, 1000 iterations per surface) across `lookup()`,
  `filings()`, `company_concept()`, `facts.snapshot()`, `daily_bars()` (yahoo and stooq),
  `document_text()`: `lookup` 1/1000 uncaught (`KeyError: 'ticker'`), `filings` 1/1000 uncaught
  (`KeyError: 'primaryDocument'`); `company_concept`, `snapshot`, both `daily_bars` sources, and
  `document_text` were all 0/1000 uncaught (only clean `FathomError` or valid output).
- `materialize()` corrupted-body probe at the ticker-map and submissions stages: both raise a
  clean `FathomError` before any file is written; no partial `<live_cache_dir>/AAPL/` directory
  is left in either case.

## Findings
See `T-024.security.md` — one new HIGH (uncaught `KeyError` on decoded-but-field-corrupted JSON
entries in `ticker_map`/`lookup`/`filings`), reachable from untrusted SEC network data.

## Gate
`uv run python scripts/check.py`: ruff check, ruff format --check, mypy (host), mypy
(`--platform linux`) all pass; **pytest fails** — `1 failed, 455 passed, 2 skipped, coverage
95.62%` — gate stops there (exit 1), secrets scan / bench / bench-drift / card-drift not
reached this run. Run independently: `uv run python scripts/secrets_scan.py` -> "no secrets
found" (exit 0); `uv run ruff check .` -> clean. `docs/SHOWCASE.md` §1's committed figure
("456 passed ... all checks passed") does not match this run and cannot, while AC2's test fails.
`git show HEAD --stat` touches only `docs/SHOWCASE.md`, `docs/tasks/T-024-sec-json-guard.md`,
`fathom/live/sec.py`, `tests/test_live_sec.py` — no `pyproject.toml`/`uv.lock` change (deps
unchanged).

# Verdict T-024 — FAIL (attempt 2)
Reviewed: 267368a9f36a8d197176f4e4dd8b4caf602afd08  Tier: T2  Attempt: 2

## Acceptance criteria
- AC1 (malformed bodies -> SOURCE_HTTP "malformed response", no body/URL leak): PASS. Own
  independent probe of the 8 specified bodies (`\xff\xfe`, `not json`, `[]`, `null`, `"s"`, `7`,
  `""`, 1 MB of `[`) against all four call sites (ticker map, submissions, an older-filings
  page, `company_concept`) is 32/32 clean `FathomError(SOURCE_HTTP, reason="malformed
  response")`, `status: 200`, no body/URL text in `message`/`details`. (An initial run flagged
  the empty-body (`b""`) case as a "leak" — that was a bug in this review's own probe: an empty
  decoded string is a substring of every string, so the naive `in` check false-positives on it.
  Manually re-verified in isolation: clean `FathomError`, nothing leaked.)
- AC2 (mutation fuzz raises only `FathomError`): PASS for the specific class attempt-1 found.
  The suite's own seeded test (seed 20240024, 200 iterations) now passes. Independent
  re-run with a different seed (999024, 2000 iterations, this review's own harness) across
  `lookup()`/`filings()`: 0/2000 uncaught. A second independent 2000-iteration byte-mutation
  fuzz (seed 999025) across `document_text()`/`company_concept()`: 0/2000 uncaught. attempt-1's
  `ticker_map`/`lookup`/`_to_filing` `KeyError`s do not reproduce; `_valid_table_rows` and the
  `ticker_map` per-entry type guard hold up under random mutation. **However**, this review's
  own *structural* (non-random-byte) probes found a **new, uncaught, sibling defect** in
  `lookup()` outside the code this task's fix touched — see Findings — so AC2 is not fully met
  in the broader sense the task intends (no uncaught exception anywhere on the `lookup`/
  `filings`/`materialize` path from a decoded-but-field-corrupted body).
- AC3 (signatures unchanged; full gate green; mypy strict; docs figure == gate): PASS on the
  gate-green and docs-match sub-clauses this run. `uv run python scripts/check.py` exits 0:
  ruff check, ruff format --check, mypy (host), mypy (`--platform linux`), pytest, secrets scan,
  bench, bench drift, card drift all pass; **`456 passed, 2 skipped, 2 warnings in 47.16s`**,
  coverage **95.34%**, followed by **"all checks passed"**. `docs/SHOWCASE.md` §1 quotes
  **"456 passed, 2 skipped, 2 warnings in 44.35s, 95.34% coverage, all checks passed"** — the
  wall-clock seconds differ (44.35s vs this run's 47.16s, expected run-to-run timing variance,
  not a fabricated figure) but every other token (pass/skip counts, coverage, verdict) matches
  exactly. AC3 met. Signatures: `ticker_map`, `lookup`, `filings`, `document_text`,
  `company_concept`, `_submissions`, `_to_filing` are byte-identical to `773bb2b`. `uv run mypy
  fathom` (host) is clean; `mypy fathom --platform linux` clean as part of the gate run.
  `grep -rn "json.loads" fathom/live` returns exactly one hit (`sec.py:138`, inside the guarded
  `_decode_json` helper; `prices.py:99` and `http.py:131` are pre-existing, unchanged,
  separately-guarded call sites, out of scope).
- Overall AC verdict: **FAIL**, on the strength of the new HIGH below — the task's stated goal
  ("no uncaught exception on a decoded-but-corrupted SEC JSON body reaches a caller") is not yet
  fully achieved; a sibling of the exact defect this task targets survives in code this diff did
  not touch.

## Fuzz summary
- AC1 malformed-body probe (8 bodies x 4 call sites = 32 cases): 32/32 clean `SOURCE_HTTP`
  "malformed response", 0 body/URL leaks (see AC1 note on the probe's own false-positive).
- `ticker_map` structural probes (entries as lists/strings, missing `ticker`/`cik_str` keys,
  wrong-typed `cik_str`/`ticker`, `cik_str` as `bool`, empty map, top-level list): 9/9 clean
  `FathomError`.
- `submissions.filings.recent`/older-page structural probes (`recent` as a list, unequal-length
  parallel arrays, numbers in place of strings, missing keys, empty table, `filings` key
  missing entirely): 6/6 clean (no uncaught exception via either `lookup()` or `filings()`).
- `company_concept` `units`-shape probes (`units` as a list, as a string, `USD` as a dict
  instead of a list, `units` missing, `units: null`): 5/5 return either a clean `FathomError` or
  the raw decoded `dict` with no crash — `company_concept` does no per-field validation by
  design (05-m4-live-data.md §4: it is a pass-through of XBRL facts for T-019's `snapshot()` to
  interpret), so a wrong-shaped `units` is not this client's contract to reject; confirmed `0/5`
  uncaught.
- Independent review fuzz (seed 999024, 2000 iterations, whole-body 1-8 byte mutation) across
  `lookup()`/`filings()`: 0/2000 uncaught. Independent review fuzz (seed 999025, 2000
  iterations) across `document_text()`/`company_concept()`: 0/2000 uncaught.
- `materialize()` corrupted-body probe (seed 999026, 200 iterations, ticker-map and submissions
  stages alternated): 0/200 uncaught, 0/200 partial `<live_cache_dir>/AAPL/` directories left.
- **Targeted structural probe outside the random-byte fuzz's reach**: `submissions.exchanges`
  as a non-empty `dict` or as an `int` — **1/1 and 1/1 uncaught `KeyError`/`TypeError`** via
  `lookup()`, and confirmed end-to-end via `materialize("AAPL", ..., force=True)`: raises a bare
  `KeyError: 0`, 0 partial cache directories left (the crash happens before `live_dir.mkdir` at
  `fathom/live/build.py:188`). See Findings.

## Findings
See `T-024.security.md` — attempt-1's HIGH (nested `entry[...]` `KeyError`s in `ticker_map`/
`lookup`/`_to_filing`) is CLOSED. One new HIGH this round: uncaught `KeyError`/`TypeError` on
`submissions["exchanges"]` in `lookup()` when that field is a wrongly-typed but valid-JSON
value, reachable from untrusted SEC network data via `materialize()`.

## Gate (verbatim summary line)
`456 passed, 2 skipped, 2 warnings in 47.16s` / `Required test coverage of 80% reached. Total
coverage: 95.34%` / `all checks passed` (full `uv run python scripts/check.py`, exit 0: ruff
check, ruff format --check, mypy host, mypy `--platform linux`, pytest, secrets scan, bench,
bench drift, card drift all green). `docs/SHOWCASE.md` §1's quoted figure matches on every
token except wall-clock seconds (44.35s committed vs 47.16s this run — expected variance).
