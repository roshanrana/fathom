# Verdict T-009 — PASS
Reviewed: fb59a49b0443ec7d399b422d7f7a991fd367ad49  Tier: T2  Attempt: 1

## Acceptance criteria

- **AC1 (CLI happy/error paths)** — PASS. Independently run with `FATHOM_AUDIT_PATH` pointed at a
  fresh temp dir, `FATHOM_DATA_DIR` at the repo fixtures, `PYTHONIOENCODING=utf-8`:
  - `fathom quote AAPL` → exit 0, `AAPL last close 332.27 (+1.75%) as of 2026-09-11`.
  - `fathom filings AAPL` → exit 0, 5 rows (10-K/10-Q with accession/date/EDGAR URL).
  - `fathom brief AAPL --json` → exit 0; output validated via
    `Briefing.model_validate_json(...)` — OK.
  - `fathom ask AAPL "main risk factors" --json` → exit 0; validated via
    `Answer.model_validate_json(...)` — OK.
  - `fathom quote ZZZZ` → exit 2, stderr `error UNKNOWN_TICKER: unknown ticker 'ZZZZ'; must be
    one of AAPL, AMZN, ...`.
  - Note: without `PYTHONIOENCODING=utf-8` the redirected JSON contained mis-decoded bytes
    (Windows console codepage mangling a curly apostrophe) — an environment requirement per the
    task's stated environment, not a code defect; confirmed clean once the var was set.

- **AC2 (`probe`)** — PASS. `FATHOM_LLM_PROVIDER=offline fathom probe` → exit 0, output
  `provider=offline model=extractive-v1 latency_ms=0` then `pong`. All three required substrings
  ("offline", "extractive-v1", "pong") present.

- **AC3 (API happy paths + status mapping)** — PASS. Independently exercised via
  `TestClient(create_app(Settings(...)))` (own script, not the shipped test file):
  - `GET /healthz` → 200, envelope keys exactly `{ok,data,error,meta}`, `data.status=="ok"`.
  - `GET /api/quote/AAPL` → 200, `data` validates as `QuoteCard`.
  - `GET /api/filings/AAPL` → 200, 5 items.
  - `POST /api/brief/AAPL` → 200, validates as `Briefing`.
  - `POST /api/ask/AAPL {"question": "main risk factors"}` → 200, validates as `Answer`.
  - `GET /api/quote/ZZZZ` → 404, `error.code == "UNKNOWN_TICKER"`.
  - `POST /api/ask/AAPL {}` → 422, `ok=False`, `data=None`.

- **AC4 (error containment)** — PASS. Monkeypatched `fathom.api.quote_card` to raise a bare
  `RuntimeError("boom-should-not-leak")`: `GET /api/quote/AAPL` → 500,
  `{"code":"INTERNAL","message":"internal error"}`; response text contains neither `"boom"` nor
  `"Traceback"`. `Settings(llm_provider="portkey", portkey_api_key=None)` on `POST /api/brief/AAPL`
  → 503, `error.code=="PROVIDER_CONFIG"`, message `"PORTKEY_API_KEY is not set"` (names the var).
  Shipped tests additionally cover an *unmapped* `FathomError` code (`DATA_MISSING`) also falling
  back to 500/INTERNAL with the detail message suppressed — verified by reading
  `tests/test_api.py::test_fr014_unmapped_fathom_error_code_also_returns_500_internal`, consistent
  with "anything else → 500" in the LLD table.

- **AC5 (meta/envelope)** — PASS. `meta.provider` reflected `settings.llm_provider` ("offline");
  every response observed (including 404/422/503/500 cases) carried all four envelope keys.

- **AC6 (mypy strict + RTM test naming)** — PASS. `uv run mypy fathom` → "Success: no issues
  found in 16 source files" (`strict = true` confirmed in `pyproject.toml`). All test functions in
  `tests/test_cli.py` are `test_fr013_*`; all in `tests/test_api.py` are `test_fr014_*` (grep for
  any non-conforming name returned nothing).

## Verification checklist

- [x] Scope respected — diff touches only `fathom/cli.py`, `fathom/api.py`,
  `tests/test_cli.py`, `tests/test_api.py`, `pyproject.toml`, `tests/test_config.py` (Orchestrator
  amendment), `docs/tasks/T-009-cli-api.md` (Handoff). No `uv.lock` change in this commit despite
  the amendment note mentioning it — harmless, since no dependency was added.
- [x] Status mapping exactly per §4: `UNKNOWN_TICKER→404`, `PROVIDER_CONFIG→503`,
  `PROVIDER_HTTP→502`, `PROVIDER_TIMEOUT→502`, `CONTRACT_INVALID→502`, validation→422, else→500
  `INTERNAL` — matches `fathom/api.py`'s `_STATUS_BY_CODE` and generic-exception handler exactly.
- [x] 500 body generic — `{"code": "INTERNAL", "message": "internal error"}` only, no exception
  text, no traceback, confirmed by direct reproduction (see AC4) and by
  `tests/test_api.py` assertions on response text.
- [x] Tests named with RTM IDs — `test_fr013_*` (FR-013, CLI) / `test_fr014_*` (FR-014, API).
- [x] No new dependencies — `pyproject.toml` diff is exactly the `[project.scripts]` block (3
  lines added, nothing else); `fastapi`/`uvicorn`/`typer` were already declared.

## Findings

- No CRITICAL/HIGH/MEDIUM issues.
- LOW/INFO: `fathom/api.py`'s route handlers are declared `async def` but call synchronous,
  potentially blocking functions (`quote_card`, `filings_for`, `brief_flow`, `ask_flow`) directly
  rather than via a thread-pool offload. These functions' (sync) signatures are inherited from
  upstream frozen contracts (T-006/T-007), not introduced by this task, so this is not a T-009
  defect — flagging only as a forward note if a future task adds a network-bound LLM provider path
  that could block the event loop under load.
- `fathom bench` / `fathom mcp` correctly print `error NOT_AVAILABLE: bench arrives in T-010` /
  `... mcp arrives in T-012` and exit 2 at this commit, matching the pinned-commit note that
  `fathom/bench.py`/`fathom/mcp_server.py` do not yet exist.

## Gate

`uv sync --all-extras` clean. Targeted suite: `uv run pytest tests/test_cli.py tests/test_api.py
tests/test_config.py -q` → 38 passed. `uv run ruff check fathom/cli.py fathom/api.py tests` → all
checks passed. `uv run mypy fathom` → success, no issues. Full gate `uv run python scripts/check.py`
→ formatting clean, ruff clean, mypy (default + `--platform linux`) clean, full pytest suite 202
passed (96.16% coverage, ≥80% required), secrets scan clean — **all checks passed**, green at this
commit as expected.
