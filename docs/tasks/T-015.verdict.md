# Verdict T-015 — FAIL
Reviewed: 96e218e61d42c57f340b75053a00fe5f4d46e965  Tier: T2  Attempt: 1

## Acceptance criteria

- AC1 (transport errors → PROVIDER_HTTP/PROVIDER_TIMEOUT, no leaks): **PASS**. Independently fuzzed
  both providers with `httpx.MockTransport` raising `ConnectError`, `RemoteProtocolError`,
  `ReadTimeout`, `ConnectTimeout`. Portkey/Anthropic `ConnectError` → `PROVIDER_HTTP`,
  `details={"status": 0, "provider": <name>, "reason": "ConnectError"}`,
  message `"<name> provider request failed (reason=ConnectError)"`; same shape for
  `RemoteProtocolError`. `ReadTimeout`/`ConnectTimeout` → `PROVIDER_TIMEOUT`,
  `details={"provider": <name>}`, message `"<name> provider request failed (reason=timeout)"`.
  No API key or base-URL (incl. query string) appeared in any message/details.

- AC2 (malformed 2xx body → PROVIDER_HTTP "malformed response", body never leaks): **FAIL**.
  Confirmed correct handling for `{"choices": []}`, `{"foo": 1}`, `"not json"` (invalid JSON),
  `{"choices": [{"message": {}}]}`, top-level `[]`, top-level `null` (and the Anthropic
  analogues `{"content": []}`, `{"content": [{}]}`) — all raise `FathomError(PROVIDER_HTTP,
  reason="malformed response")` with no body leakage. However, the case explicitly listed in
  the verification procedure — `{"choices": [{"message": {"content": null}}]}` (Portkey) and its
  Anthropic analogue `{"content": [{"type": "text", "text": null}]}` — raises an **uncaught
  `pydantic.ValidationError`**, not `FathomError`. Reproduction:
  ```
  handler returns 200 {"choices": [{"message": {"content": null}}]}
  provider.complete_json(...) raises:
    pydantic_core._pydantic_core.ValidationError: 1 validation error for ProviderResult
    text: Input should be a valid string [type=string_type, input_value=None, ...]
  ```
  Root cause: `_parse_portkey_payload`/`_parse_anthropic_payload` only catch
  `(json.JSONDecodeError, KeyError, IndexError, TypeError)` around the dict/index lookups. When
  the key exists but its value is `None`, no lookup exception is raised — `text` is bound to
  `None` and returned successfully from the parse helper. The `ValidationError` is raised later,
  in `ProviderResult(text=text, ...)` construction, outside any `try`/`except` in
  `complete_json`, so it escapes the provider as a raw (non-`FathomError`) exception. This is a
  D-010 violation ("No raw ... exception[s] escape[s] a provider") and a gap against the pack's
  own AC2 wording ("each raise `PROVIDER_HTTP` with reason `malformed response`"). It is also
  not covered by any test in `tests/test_providers.py` — the implementer's malformed-body
  parametrizations omit the null-content shape entirely, even though it is the shape most likely
  to occur in practice (a real 2xx response with an empty/refusal completion).

- AC3 (CLI `brief AAPL` w/ Portkey ConnectError → exit 2, exact stderr): **PASS**. Independently
  reproduced via `CliRunner` with a monkeypatched `fathom.providers.httpx.Client` returning an
  `httpx.MockTransport` that raises `ConnectError`, `FATHOM_LLM_PROVIDER=portkey`,
  `PORTKEY_API_KEY=x`, `PORTKEY_BASE_URL=http://127.0.0.1:9/v1` (no real socket opened): exit code
  2, output exactly `error PROVIDER_HTTP: portkey provider request failed (reason=ConnectError)\n`
  on the error stream (`fathom/cli.py:47-48`, `typer.echo(..., err=True); raise typer.Exit(2)`).

- AC4 (`fathom ask AAPL "Should I buy Apple stock?"` offline → guarded claim renders
  `(no source)`; JSON unchanged): **PASS**. Independently ran offline (temp audit path): text
  rendering line is
  `- [removed: recommendation-style language is not permitted in Fathom output] [removed] (no source)`
  (no `? ? ?`); `--json` rendering unchanged — `claims[0].guarded == true`,
  `claims[0].source.accession == ""`.

- AC5 (existing tests pass; mypy strict; full gate; RTM-named tests): **PASS** on the letter of
  the criterion, but see Findings — the full gate does not exercise the AC2 gap above since no
  test covers the null-content shape.

## Verification checklist

- [x] scope respected — diff touches exactly `fathom/providers.py`, `fathom/cli.py`,
      `tests/test_providers.py`, `tests/test_cli.py`, and the pack itself
      (`docs/tasks/T-015-provider-errors.md`); no other files changed.
- [ ] no raw httpx/KeyError/IndexError escapes — **violated**: a raw `pydantic.ValidationError`
      escapes both `PortkeyProvider.complete_json` and `AnthropicProvider.complete_json` when a
      2xx body has the expected keys/indices but a `null` value where a string is expected (see
      AC2 above). This is a raw, uncaught exception of a different type than the ones the
      implementer's `except` clauses anticipate, and D-010 (`03-lld.md §2.7`) requires no raw
      exception of any kind escape a provider.
- [x] no key, URL query or body in any message/details/log — confirmed by fuzz across all
      transport-error and malformed-body cases (including the failing one above: the
      `ValidationError` itself does not include the secret key or body either, though it is not a
      `FathomError` at all).
- [x] tests named with RTM-id prefixes (`test_fr010_*`, `test_fr013_*`, `test_nfr002_*`) matching
      the pack's `rtm: [FR-010, FR-013, NFR-002]`; no new dependencies (`pyproject.toml`/`uv.lock`
      untouched in this commit).

## Findings

**CRITICAL** — `fathom/providers.py`: `_parse_portkey_payload` (used by `PortkeyProvider`) and
`_parse_anthropic_payload` (used by `AnthropicProvider`) do not guard against a present-but-`None`
completion-text value. A 2xx body such as `{"choices": [{"message": {"content": null}}]}`
(Portkey) or `{"content": [{"type": "text", "text": null}]}` (Anthropic) passes the
`try`/`except (json.JSONDecodeError, KeyError, IndexError, TypeError)` block unharmed (`text =
None` is a valid assignment, no exception raised at that point), and the resulting
`pydantic.ValidationError` from `ProviderResult(text=None, ...)` is raised outside any
`FathomError`-producing guard, propagating as a raw exception out of `complete_json`. This is
exactly the class of case D-010 and the pack's AC2 are meant to close, and it is the case most
plausible in production (a gateway returning HTTP 200 with an empty/null completion, e.g. a
content-filtered or truncated response). Fix: validate that the extracted `text` is a non-`None`
`str` inside the existing `try` block (e.g. add an explicit check that raises via the same
malformed-response path, or include the emptiness check before constructing `ProviderResult`) for
both parse helpers.

No other findings. All other tested transport-error and malformed-body shapes, the CLI exit-2
path, and the CLI `(no source)`/JSON-unchanged rendering behave exactly per spec and the pack's
message forms.

## Gate

`uv sync --all-extras`: clean. `uv run pytest tests/test_providers.py tests/test_cli.py -q`: 51
passed. `uv run mypy fathom`: no issues (18 source files). `uv run python scripts/check.py`: all
checks passed (ruff, ruff format, mypy fathom + mypy fathom --platform linux, pytest 257 passed /
95.64% coverage, secrets scan clean, bench/bench-drift/card-drift clean). The gate is green, but
it does not detect the AC2 gap because no existing or added test exercises a present-but-null
completion-text value — this is a hole in test coverage, not a false gate pass.
