# Verdict T-016 — PASS

Reviewed: c7beea3a11a0cb3e76f79fa76e40f049e6f86c60  Tier: T2  Attempt: 1

## Acceptance criteria

- **AC1 (newline-tail, business_snapshot):** PASS. Independent synthetic 10-K:1 excerpt
  `"Business\nCompany Background\nThe Company designs, manufactures and markets smartphones and
  related services worldwide."` → `business_snapshot[0].text == quote ==` "The Company designs,
  manufactures and markets smartphones and related services worldwide." (heading fragment
  dropped).
- **AC2 (boilerplate skip, latest_results):** PASS. Independent synthetic 10-Q:I.2 excerpt with
  three boilerplate sentences (forward-looking / safe harbor / "should be read in conjunction")
  followed by "Net sales increased 5% year over year driven by services growth in all segments."
  → `latest_results[0].text` equals exactly that fourth sentence; none of the three boilerplate
  sentences appear anywhere in `latest_results`.
- **AC3 (dedup 10-K:3 / newest 10-Q:II.1):** PASS. Identical legal-proceedings sentence supplied
  in both sections → appears exactly once in `notable_disclosures`.
- **AC4 (Item-prefix trim):** PASS. `"Item 1A. Risk Factors The following summarizes factors that
  could affect the Company."` → claim text `"Risk Factors The following summarizes factors that
  could affect the Company."` (no "Item 1A." prefix); `quote == text` and `quote` is a verbatim
  substring of the excerpt.
- **AC5 (offline brief() over 20 tickers):** PASS. Ran `brief()` for all 20 `UNIVERSE` tickers
  independently (own temp audit path): `verified_share == 1.0` for every ticker and overall;
  per-ticker `claims_total` ranges 11–21 (all ≥ 10, PFE lowest at 11). AAPL "Business snapshot"
  first claim: "The Company designs, manufactures and markets smartphones, personal computers,
  tablets, wearables and accessories, and sells a variety of related services." — no heading
  fragment. AAPL "Latest results" first claim on the real newest 10-Q (`0000320193-26-000013`):
  "Unless otherwise stated, all information presented herein is based on the Company's fiscal
  calendar…" — traced the raw excerpt sentence-by-sentence and confirmed the five preceding
  forward-looking / safe-harbor / Item-cross-reference sentences are all correctly flagged
  boilerplate and skipped; the surviving first claim contains none of the D-011(b) marker
  phrases, so it is no longer forward-looking boilerplate per the spec's literal marker list.
- **AC6 (formatting):** PASS. `format_market_cap(4_849_208_188_600) == "$4.85 T"`,
  `format_market_cap(412_000_000_000) == "$412 B"`, `format_market_cap(95_000_000) == "$95 M"`,
  `format_market_cap(None) == "—"` (all checked independently). Fresh `AppTest` run of
  `app/main.py`: exactly 6 `st.metric` cards, no metric value contains "…", exactly one caption
  starting with "Prices:" present (`"Prices: AlphaDojo/dojo_stock_kline via data/bars.parquet as
  of 2026-09-11 · Snapshot: AlphaDojo/dojo_quote via data/quotes.parquet as of 2026-09-11 16:00
  UTC"`), containing both sources and both as-of stamps. Grep of `app/` for `read_parquet`,
  `BM25`, `re.compile`: no matches.
- **AC7 (full gate + test naming):** PASS. `uv run python scripts/check.py` → "all checks
  passed" (ruff check, ruff format --check, mypy fathom, mypy fathom --platform linux, pytest
  [263 passed, 95.99% coverage], secrets scan, bench, bench drift, card drift all green — no
  dirty diff at this commit). Tests named `test_fr002_*` (2), `test_fr006_*` (5),
  `test_fr012_*` (6, pre-existing + updated) all present in `tests/test_app.py` /
  `tests/test_providers.py`.

## Verification checklist

- [x] Scope respected — only `fathom/providers.py`, `tests/test_providers.py`,
      `app/components/rendering.py`, `tests/test_app.py`, `metrics/{headline.json,card.json,
      card.md}`, and the pack's own Handoff section were touched; no scope creep.
- [x] Quotes remain verbatim substrings — `verified_share == 1.0` confirmed both by the full
      gate and an independent 20-ticker `brief()` run.
- [x] No computation added to `app/` beyond formatting — `rendering.py` diff adds only
      `format_market_cap` (arithmetic scaling/string formatting) and `_metrics_source_caption`
      (string interpolation); no filing/claim/dedup logic added to `app/`.
- [x] Metrics regenerated; drift green — `metrics/headline.json` claims_total 378→374
      (verified_share unchanged at 1.0), `metrics/card.json`/`card.md` regenerated; `bench
      drift` and `card drift` gate steps both green at HEAD.
- [x] Tests named with RTM IDs; no new dependencies — `test_fr002_*`/`test_fr006_*` added;
      `pyproject.toml` and `uv.lock` unchanged in this commit.

## Findings

No issues found. All four D-011 heuristics (a)-(d) verified independently against fresh
synthetic fixtures (not reusing the implementer's own test bodies), plus a full 20-ticker
offline run and a fresh `AppTest` render. Gate is fully green including bench/card drift at
this exact commit.

## Gate

`uv sync --all-extras` ✓ · `pytest tests/test_providers.py tests/test_app.py -q` → 48 passed ✓ ·
`mypy fathom` → no issues (18 files) ✓ · `scripts/check.py` → all checks passed (263 tests,
95.99% coverage, bench drift clean, card drift clean) ✓
