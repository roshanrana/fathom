# Security review T-006 — FINDINGS
Boundary: B1, B3 (B2, B4 touched)  Risk class: medium
1. [HIGH] `Claim.text` max_length=600 is enforced only on the frozen output contract (`fathom/contracts.py`), not on the provider-facing draft contract validated during parse/repair (`BriefingDraft`/`DraftClaim` have no length cap). A provider response with an over-long claim `text` passes `_parse_draft`/`BriefingDraft.model_validate` cleanly, then `_to_claim` (fathom/briefing.py) constructs `Claim(...)` and raises an **unhandled** `pydantic.ValidationError` — reproduced locally (600-char boundary confirmed). This call isn't wrapped in try/except, so it bypasses the documented one-repair→`CONTRACT_INVALID` flow (LLD §2.10) entirely: an adversarial or merely verbose model output (B3) crashes `brief()` outside the `FathomError` taxonomy instead of following the designed contract-validation path — control C-22 — fix required before merge (add the same `max_length` to `DraftClaim.text`, or catch `ValidationError` around `_claims_from_draft`/`_to_claim` and re-raise as `CONTRACT_INVALID`).
2. [LOW] The repair-retry error text (`_obtain_draft`, `error_text = str(exc)[:300]`) is built from `str(ValidationError)`, whose default rendering can embed a truncated `repr()` of the offending field's own `input_value` (confirmed: a long `text` value shows up as `input_value={'text': 'IGNORE ALL PRIO...`). `json.JSONDecodeError` does not do this (position-only). So the `{"repair": "<error text>"}` sent back to the model (LLD §2.10) can carry a short, truncated echo of the model's own prior draft content, not purely "error text" — bounded to ≤300 chars, contains only the model's own already-produced text (never a secret/key), so this matches the threat model's accepted B3 residual ("other effects limited to content quality") rather than a boundary breach — control C-22 — accepted candidate (tighten by building the repair message from `exc.errors()` fields only, e.g. `loc`/`type`, and dropping `input_value`).
3. [LOW] A `.tmp-audit/log.jsonl` file appeared untracked in the live repo tree (`C:\Code-Central\fathom\.tmp-audit\`, not the worktree) during this wave, containing one audit line (ticker MCD, claims_total=8, matching this task's documented MCD/AC5 gap). Grep confirms no file in this diff references `.tmp-audit` as a literal path, and every test added here (`tests/test_briefing.py`, `tests/fakes.py`) correctly scopes `audit_path` under pytest's `tmp_path` fixture — so this diff's own tests do not write outside `tmp_path`; the artifact looks like a manual/ad-hoc run (custom `FATHOM_AUDIT_PATH`) rather than this task's test suite — control C-11 — accepted candidate (not attributable to T-006's tests; recommend `.gitignore`-ing `.tmp-audit/` and confirming no ad-hoc runs write into the live tree).

Verified clean: raw provider text is never logged and never placed in `FathomError`/`CONTRACT_INVALID` messages or `details` (`{"attempt": 2}` only, no body) — CONTRACT_INVALID satisfies check (2). `json.loads` on model output is plain parsing (no `eval`/`exec`); `BriefingDraft`/`DraftClaim` set `extra="ignore"`, dropping unknown keys. `verify_claim`/`_to_claim` looks up `(accession, section_id)` only in the in-memory `context.excerpts` dict built from already-loaded excerpts — an unknown pair yields `verified=False` with no exception and no file I/O. `audit.record` hashes prompt/response with sha256 always, drops the bodies unless `audit_bodies=True`; the one log line is `audit written path=%s`. The `brief` log line matches LLD §9 verbatim (`ticker`, `provider`, `latency_ms`, `claims`, `verified`, `guard_hits` only). `BriefingContext.to_user_json()` emits only `task`/`ticker`/`company`/`as_of`/`filings`/`excerpts` (public filing fields) — no settings, no keys. Commit touches only `fathom/briefing.py`, `tests/fakes.py`, `tests/test_briefing.py`, `docs/tasks/T-006-briefing.md` — no `pyproject.toml`/`uv.lock` change (deps unchanged).
Scanner summary: secrets 0, ruff 0/0 (fathom/briefing.py, all checks passed), deps unchanged: yes
