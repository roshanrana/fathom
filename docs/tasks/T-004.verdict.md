# Verdict T-004 — PASS
Reviewed: 03df3475d8dec23b47bbfa4a9b68a8e8c78023a0  Tier: T2  Attempt: 1

## Acceptance criteria
- AC1 (Portkey request/response shape): PASS. `uv run pytest` test
  `test_fr010_portkey_provider_posts_expected_request_and_parses_response` plus an independent
  `httpx.MockTransport` handler confirmed URL
  `https://portkeygateway.perficient.com/v1/chat/completions`, header `x-portkey-api-key` equal
  to the key, JSON body with `model`, two messages (system, user), `max_tokens`,
  `temperature: 0.1`; parsed `choices[0].message.content` and `usage.prompt_tokens` /
  `completion_tokens`; missing `usage` yields `None`/`None`; `latency_ms >= 0`.
- AC2 (Anthropic request/response shape): PASS. Independent `MockTransport` confirmed POST to
  `https://api.anthropic.com/v1/messages` with `x-api-key`, `anthropic-version: 2023-06-01`,
  body `system` + one user message + `max_tokens`; parsed `content[0].text` and
  `usage.input_tokens`/`output_tokens`.
- AC3 (error taxonomy, no leakage): PASS. Independent 500 response (with a deliberately leaky
  body and a live key) raised `FathomError(PROVIDER_HTTP)` with `details == {"status": 500,
  "provider": "portkey"}`; message/details contained neither the key nor the response body nor a
  URL query. `httpx.ReadTimeout` raised `PROVIDER_TIMEOUT`.
- AC4 (PROVIDER_CONFIG before client use): PASS. `make_provider(Settings(llm_provider="portkey"))`
  / `"anthropic"` with no key raised `PROVIDER_CONFIG` naming `PORTKEY_API_KEY` /
  `ANTHROPIC_API_KEY`, and a `MockTransport` handler wired to raise `AssertionError` if invoked was
  never called (confirmed independently, not just via implementer's test).
- AC5 (OfflineProvider briefing, §6.3): PASS. Independent synthetic 10-K
  (`10-K:1/1A/1C/3/7`) + two 10-Qs (`10-Q:I.2` old/new, `10-Q:II.1`) produced all six keys with
  counts 3/4/4/≤3/≤3/≤4; used the newest 10-Q (`filing_date`-sorted) for `latest_results`, not the
  older one; ALL-CAPS and ≥4-numeric-token ("table row") sentences were skipped from `risks`;
  every claim's `quote` is a substring of its excerpt's text, `quote == text` except
  `talking_points` (whose `text` starts with `"From "`).
- AC6 (offline ask / probe): PASS. Ask on 4 excerpts returned ≤3 claims; ask on excerpts with no
  qualifying sentence returned exactly `{"claims": [], "not_found": true}`; probe
  (`{"ping": true}`) returned `"pong"`.
- AC7 (log line format, no key in logs): PASS. A capturing `logging.Handler` on logger `"fathom"`
  observed exactly `record.msg == "provider selected name=%s model=%s"` with
  `args == (name, model)`; a run with a live secret key in `Settings` produced no log record
  containing that key string.
- AC8 (SECTION_CAPS / CANONICAL_SECTIONS / prompt fidelity / mypy / test names): PASS.
  `SECTION_CAPS` has the 12 LLD §6.2 values exactly; `CANONICAL_SECTIONS` has the 12 §2.5-table
  entries in table order; `SYSTEM_BRIEFING` and `SYSTEM_ASK` are byte-for-byte equal (verified
  programmatically, including the en-dash in "2–4") to strings reconstructed from LLD §6.1/§6.5 —
  `SYSTEM_ASK` = rules 1–4 of `SYSTEM_BRIEFING` plus the ask-specific sentence and schema, per the
  (empty) handoff's stated construction; `mypy fathom` strict clean; all new tests are named
  `test_fr010_*` / `test_nfr005_*` / `test_nfr010_*`.

## Verification checklist
- [x] scope respected — diff touches only `fathom/prompts.py`, `fathom/providers.py`,
      `tests/test_prompts.py`, `tests/test_providers.py` (confirmed via `git show --stat` /
      `--name-only`); `prompts.py` does not import `fathom.filings`.
- [x] error taxonomy used; no key value in any message, log, or exception (independently probed
      with distinct secret strings for Portkey and Anthropic; none appeared in error text,
      details dict, or log records).
- [x] PROVIDER_CONFIG raised before any client construction (confirmed: `MockTransport` handlers
      wired to raise on any call were never invoked in the missing-key paths).
- [x] tests named with RTM IDs — `test_fr010_*`, `test_nfr005_*`, `test_nfr010_*` throughout both
      new test files.
- [x] no new dependencies — `pyproject.toml` and `uv.lock` are untouched by this commit; only
      `httpx`, `pydantic`, stdlib (`json`, `logging`, `re`, `time`, `typing`) imported.
- [x] prompt text matches LLD §6 exactly — confirmed via exact string equality against
      reconstructed §6.1/§6.5 text (see AC8).

## Findings
No blocking or non-blocking findings. The Handoff section of the task pack was left empty by the
implementer; this is a process nit only (not a checklist item) and did not impede verification —
all behavior was independently re-derived from the LLD and confirmed by direct execution rather
than by trusting implementer narrative.

## Gate
`uv run python scripts/check.py`: all checks passed (ruff check, ruff format --check, mypy fathom,
mypy fathom --platform linux, pytest, secrets scan). Full suite: 73 passed, 0 failed. Coverage:
96.52% overall (`fathom/prompts.py` 100%, `fathom/providers.py` 95%), required 80% minimum met.
Targeted `uv run pytest tests/test_providers.py tests/test_prompts.py -q`: 28 passed.
