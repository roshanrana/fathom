# Security review T-005 — CLEAR
Boundary: B1, B4  Risk class: medium
1. [LOW] `scrub_claims`/`is_advice` scan only `Claim.text`, never `Claim.quote` (matches the frozen §2.8 design). An unverified claim (`verify_claim` returned `False`, e.g. the model fabricated a "quote" that isn't literally in the filing) still carries that unguarded `quote` string through to `Briefing`/`Answer` with no advice-pattern check applied to it — control C-09 — accepted candidate now (`briefing.py`/`ask.py` don't exist yet in this diff); flag for T-006/T-007 review to confirm the UI/API never renders `quote` for a claim with `guarded=False` and `verified=False` without the same guard, or extend the guard to `quote`.
2. [LOW] The advice guard is plain regex over casefolded/curly-quote-normalised text with no Unicode confusable/homoglyph normalisation (e.g. Cyrillic look-alikes), so a determined user could phrase advice-eliciting text to dodge all 14 patterns — control C-09 — accepted candidate; matches the threat model's own residual-risk note ("model may still phrase borderline language; flagged not hidden", B1 T/E row), not a regression introduced by this diff.

Verified clean: all 14 `ADVICE_PATTERNS` use only bounded quantifiers (no nested/overlapping repetition) — `is_advice` on a 200 kB "should should should buy" string and 200 kB of "a"*/mixed input each returned in ~0.03–0.06 s, no backtracking blowup; guard never calls `eval`/`exec` and is applied to `.text` only (never the raw excerpt or a dynamically-evaluated expression); `GUARD_NOTICE` fully replaces `claim.text` (also sets `guarded=True`, `verified=False`) and never echoes the original text — `scrub_claims` returns a new list, input list/claims are never mutated (asserted by test snapshot). `verify_claim` checks `6 <= word_count <= 60` (via `.split()`) *before* the substring check, so an empty or whitespace-only quote (`word_count == 0`) is rejected before it could trivially match `normalise(section_text)`. Audit `record()`: default mode (`audit_bodies=False`) pops `prompt`/`response` before serialising; `AuditRecord` has no field that could carry a key (only hashes/counts/metadata); file opened `"a"` (append-only), path taken only from `settings.audit_path` (from `FATHOM_AUDIT_PATH`/default, never per-request user input); `OSError` → `FathomError(Code.AUDIT_WRITE)` with message/`details` naming only the path, never the record; the one success log line `audit written path=%s` runs only after the write succeeds and logs nothing but the path. `Claim.text` is `Field(max_length=600)`; `DraftClaim`/`BriefingDraft`/`AnswerDraft` all set `model_config = ConfigDict(extra="ignore")`, so extra provider-supplied fields are dropped, not stored. Commit touches only `fathom/audit.py`, `fathom/contracts.py`, `fathom/guard.py`, `tests/test_audit.py`, `tests/test_contracts.py`, `tests/test_guard.py`, `docs/tasks/T-005-contracts-guard-audit.md` — no `pyproject.toml`/`uv.lock` change.
Scanner summary: secrets 0, ruff 0/0 (guard.py, audit.py, all checks passed), deps unchanged: yes
