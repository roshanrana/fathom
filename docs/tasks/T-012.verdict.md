# Verdict T-012 — PASS
Reviewed: a268fba98e9ab28b0ccd880b4108a1ddc8808dcc  Tier: T2  Attempt: 1

## Acceptance criteria

- **AC1 — PASS**: In-process session (`create_connected_server_and_client_session`) lists exactly
  `{get_quote, list_filings, get_briefing, ask_filings}`. Every description ends with
  `Settings().disclaimer` verbatim (independently checked outside the implementer's own test, same
  result). Descriptions are built once at import time from `Settings()` (not `.from_env()`), which
  matches the AC's literal wording exactly.
- **AC2 — PASS**: With `FATHOM_AUDIT_PATH` pointed at a fresh temp dir and `FATHOM_DATA_DIR` at the
  repo's real fixtures, direct calls (bypassing the test suite): `get_quote("AAPL")` →
  `QuoteCard.model_validate_json` OK; `list_filings("AAPL")` → 5 items, each validates as `Filing`;
  `get_briefing("AAPL")` → `Briefing.model_validate_json` OK; `ask_filings("AAPL", "main risk
  factors")` → `Answer.model_validate_json` OK; `get_quote("ZZZZ")` →
  `{"ok": false, "error": {"code": "UNKNOWN_TICKER", ...}}`, no exception raised.
- **AC3 — PASS (fallback used, as allowed by the Handoff)**: Implementer used
  `mcp.shared.memory.create_connected_server_and_client_session` against
  `mcp_server.server._mcp_server` instead of real stdio spawn, citing Windows stdio-spawn
  unreliability — this is the AC's documented fallback ("if stdio spawning is unreliable on
  Windows, the test may instead exercise `server._tool_manager.list_tools()`... and note it in the
  handoff"); the Handoff does note it. The in-process session lists exactly 4 tools (verified
  independently, not just via the shipped test).
- **AC4 — PASS**: `uv run mypy fathom` → "Success: no issues found in 18 source files" (strict =
  true, confirmed in `pyproject.toml`). All 8 tests in `tests/test_mcp.py` are named `test_fr019_*`.
  `tests/test_mcp.py` opens with `pytest.importorskip("mcp")` before any `mcp`-touching import
  (stdlib imports only precede it), so the module skips cleanly when `mcp` is absent. Tools never
  raise: independently exercised `ask_filings("AAPL", "")` (empty question) and
  `ask_filings("AAPL", "x"*5000)` (5000-char question) and `ask_filings("ZZZZ", "anything")`
  (unknown ticker) — all returned JSON text, no exception.

## Verification checklist

- [x] Scope respected — diff touches exactly `fathom/mcp_server.py`, `tests/test_mcp.py`,
      `docs/mcp.md`, plus the pack file itself (`docs/tasks/T-012-mcp-server.md`, Handoff section).
      No changes to `pyproject.toml` or `uv.lock`.
- [x] Tools never raise; error envelope used — every tool wraps its flow call in
      `try/except FathomError` → `_error_json`; independently confirmed with edge-case inputs above.
- [x] Disclaimer on every description — all four end with `Settings().disclaimer`.
- [x] Tests named with RTM IDs — all 8 tests in `tests/test_mcp.py` are `test_fr019_*`.
- [x] No new dependencies beyond the declared `mcp` group — `mcp>=1.9,<2` was already declared by
      T-000; no `pyproject.toml`/`uv.lock` diff in this commit.

## Findings

No CRITICAL/HIGH/MEDIUM issues. Implementation matches `03-lld.md` §4 verbatim (four tools,
`model_dump_json()` contracts, error envelope, disclaimer-suffixed descriptions,
`fathom/mcp_server.py: main() -> None` running `server.run()`).

## Gate

- `uv sync --all-extras`: OK.
- `uv run pytest tests/test_mcp.py -q`: 8 passed.
- `uv run ruff check fathom/mcp_server.py tests/test_mcp.py`: All checks passed.
- `uv run mypy fathom`: Success, no issues (18 source files).
- `uv run python scripts/check.py` (full gate): ruff check, ruff format --check, mypy fathom, mypy
  fathom --platform linux all pass. pytest: 224 passed, 1 skipped, **1 failed** —
  `tests/test_cli.py::test_fr013_mcp_reports_not_available_before_t012_ships`, the known stale
  T-009 stub (fails with `ValueError: I/O operation on closed file` inside Typer's `CliRunner`
  because `fathom mcp` now really dispatches to `mcp_server.main()`/`server.run()`). This is exactly
  the single failure disclosed in the Handoff, explicitly out of scope for T-012 (cli.py/test_cli.py
  belong to T-013), and is treated as out of scope per the verification brief. No other test failed.
  Coverage: 95.06% (≥ 80% gate).
