# Verdict T-000 — FAIL
Reviewed: 619bb7274c0000ecd7678be991cae7f5117a9a39  Tier: T2  Attempt: 1
## Acceptance criteria
- AC1: PASS — `uv run python scripts/check.py` exited 0 (all checks passed, "all checks passed" printed); `.github/workflows/check.yml` runs `actions/checkout@v4`, `astral-sh/setup-uv@v5`, `uv python install 3.12`, `uv sync --all-extras`, `uv run python scripts/check.py` on `ubuntu-latest`.
- AC2: PASS — verified live via `python -c`: `Settings.from_env({})` → `llm_provider="offline"`, matches LLD defaults; `from_env({"FATHOM_LLM_PROVIDER":"portkey","PORTKEY_API_KEY":"k"})` → `llm_provider="portkey"`, `portkey_api_key="k"`; `from_env({"FATHOM_LLM_PROVIDER":"nope"})` raised `FathomError` with `code=PROVIDER_CONFIG`; `FATHOM_AUDIT_BODIES` in {"1","true","yes","TRUE","Yes"} all → `True` (fathom/config.py:45-46, `_parse_bool`/`_TRUE_VALUES`).
- AC3: PASS — `require_ticker("aapl") == "AAPL"`; `require_ticker("ZZZZ")` raised `UNKNOWN_TICKER`, message contains all 20 `UNIVERSE` tickers (verified: `missing tickers in message: []`). fathom/data.py:42-50.
- AC4: PASS — `company("MSFT", data_dir).name == "Microsoft Corporation"`, `.exchange == "NasdaqGS"`; `load_frame("bars", data_dir)` columns exactly `['symbol','date','open','high','low','close','volume']`; `data/*.parquet` total = 11,838,276 bytes (~11.29 MB) ≤ 20 MB. tests/test_data.py:28-45 plus live re-execution.
- AC5: PASS — `AppTest.from_file("app/main.py").run()` → `at.exception` empty (`ElementList()` = no exceptions), 1 selectbox present, default selection AAPL, page text contains "Apple Inc." and the full `DEFAULT_DISCLAIMER` string.
- AC6: **FAIL** — pyproject.toml declares `mcp` and `screenshots` as `[dependency-groups]` (PEP 735) rather than `[project.optional-dependencies]`. `uv sync --all-extras` — the exact command mandated by AC1/`.github/workflows/check.yml` — only installs `[project.optional-dependencies]` extras (confirmed via `uv sync --help`: "`--all-extras` Include all optional dependencies" vs the separate `--all-groups`/`--group` flags for dependency-groups). Live check after `uv sync --all-extras`: `uv pip list` shows no `mcp` or `playwright` package installed. `dev` was installed only because uv auto-includes the group literally named `dev`; `mcp`/`screenshots` get no such treatment. Runtime + dev dependency set otherwise matches LLD §1 exactly; mypy strict config (`plugins=["pydantic.mypy"]`, `ignore_missing_imports` scoped to `streamlit.*`/`plotly.*`) matches LLD §1 exactly; `[project.scripts]` correctly omitted per the pack.
- AC7: PASS — coverage of `fathom/` = 98.89 % (≥ 80 %, `pytest --cov=fathom --cov-fail-under=80` in the gate); `grep -rn "print(" fathom/` → none found (also asserted by `test_nfr003_no_print_statements_in_package`); `README.md` exists with purpose + three run commands.

## Verification checklist
- [ ] scope respected (see F1, non-blocking) - [x] error taxonomy used - [x] no sensitive logging; secrets scan runs in check.py - [x] tests named with RTM ids - [x] no new dependencies beyond LLD §1 (see F2 re: mechanism) - [ ] CI workflow equivalent to local gate (see F2)

## Findings
1. (non-blocking) `docs/tasks/T-000-foundation-skeleton.md` was modified (Handoff section, +6 lines) — outside the task's literal Scope file list, but this is the pack's own "Handoff (Implementer fills, ≤10 lines)" template section, not deliverable code; content is accurate (25 tests, coverage 98.89 % both independently reproduced).
2. (blocking) AC6 / CI equivalence: `mcp` and `screenshots` optional groups live under `[dependency-groups]`, which `uv sync --all-extras` does not install (verified: `uv pip list` shows neither package present after a full `--all-extras` sync). The LLD calls these "optional groups" specifically in the context of a `uv sync --all-extras && uv run python scripts/check.py` gate; as declared, that gate can never exercise mcp/playwright code. Any later task (T-011 screenshots, T-012 mcp-server) that adds code importing `mcp` or `playwright` under `fathom/`/`scripts/` will fail mypy/pytest in this exact CI workflow unless the workflow or pyproject mechanism is fixed — and `.github/workflows/check.yml` is this task's file to get right. Fix: move `mcp` and `screenshots` to `[project.optional-dependencies]`, or change the frozen CI command to `uv sync --all-extras --all-groups`.

## Gate
check.py: all checks passed (ruff check, ruff format --check, mypy fathom ×2, pytest --cov=fathom --cov-fail-under=80, secrets scan); pytest: 25 passed; coverage: 98.89 %.

# Verdict T-000 — PASS (attempt 2)
Reviewed: bad10591ecfb7685261d37b5c0a3eb024004caac  Tier: T2  Attempt: 2
## Acceptance criteria
- AC1: PASS — `uv sync --all-extras` succeeded; `uv run python scripts/check.py` → "all checks passed" (exit 0); `.github/workflows/check.yml` unchanged since attempt 1, still runs `actions/checkout@v4`, `astral-sh/setup-uv@v5`, `uv python install 3.12`, `uv sync --all-extras`, `uv run python scripts/check.py` on `ubuntu-latest` — equivalent to the local gate just reproduced.
- AC2: PASS — unchanged from attempt 1 (fathom/config.py not touched by attempt 2); `tests/test_config.py::test_fr001_*`, `test_nfr005_*` re-ran green under the new dependency set (15 passed in `tests/test_config.py`).
- AC3: PASS — unchanged from attempt 1 (fathom/data.py not touched); `tests/test_data.py` re-ran green (6 passed).
- AC4: PASS — unchanged from attempt 1; `tests/test_data.py` re-ran green (company/frame/size assertions included in the 6 passed).
- AC5: PASS — unchanged from attempt 1 (app/main.py not touched); `tests/test_app_skeleton.py` re-ran green (1 passed).
- AC6: **PASS (fixes F2)** — `pyproject.toml` now declares `[project.optional-dependencies]` with `mcp = ["mcp>=1.9,<2"]` and `screenshots = ["playwright>=1.45"]`; `[dependency-groups]` retains only `dev` (ruff, mypy, pytest, pytest-cov, pandas-stubs, pyarrow-stubs). This is an exact match to LLD §1's "Dependencies" paragraph (runtime set, optional groups, dev set) and its mypy strict/`plugins=["pydantic.mypy"]`/`ignore_missing_imports` (streamlit.*, plotly.*) config, both unchanged and still exact. Live re-verification: `uv sync --all-extras` then `uv pip list | grep -Ei "^mcp |^playwright "` → `mcp 1.30.0`, `playwright 1.62.0`, both present. `[project.scripts]` still correctly absent (`'fathom = "fathom.cli:app"'` not in file, also asserted by `tests/test_config.py::test_nfr003_pyproject_declares_lld_dependency_set`, which now asserts membership in `project["optional-dependencies"]` rather than `dependency-groups` — the assertion that previously encoded the bug is fixed).
- AC7: PASS — `fathom/` coverage = 98.40 % in this gate run (≥ 80 %; total includes T-001's `fathom/quotes.py`, present in this wave's tree but out of scope for this review); T-000-scope files individually: config.py 100 %, data.py 97 %, errors.py 100 %. `grep -rn "print(" fathom/` → no matches (also asserted by `test_nfr003_no_print_statements_in_package`, unchanged, still passing). README.md unchanged, still present with purpose + three run commands.

## Verification checklist
- [x] scope respected — attempt-2 commit (`bad1059`) touches exactly `pyproject.toml`, `uv.lock`, `tests/test_config.py`, and `docs/tasks/T-000-foundation-skeleton.md` (the pack's own Handoff section, +1 line); no other files. F1 from attempt 1 (Handoff edit) is the same class of non-blocking note, not a new issue.
- [x] error taxonomy used — unchanged from attempt 1, not touched by this attempt.
- [x] no sensitive logging; secrets scan runs in check.py — reconfirmed, "no secrets found" in this run.
- [x] tests named with RTM ids — unchanged; `test_config.py` retains `test_fr001_*`/`test_nfr003_*`/`test_nfr005_*` naming after the edit.
- [x] no new dependencies beyond LLD §1 — `mcp`/`screenshots` package specs (`mcp>=1.9,<2`, `playwright>=1.45`) are byte-identical to attempt 1, only their pyproject table changed (`[dependency-groups]` → `[project.optional-dependencies]`); LLD §1 match confirmed above.
- [x] CI workflow equivalent to local gate — `.github/workflows/check.yml` untouched by attempt 2; the mechanism fix in pyproject.toml now makes the existing `uv sync --all-extras` step in that workflow actually install `mcp`/`playwright`, closing the gap F2 identified. Workflow file itself needed no edit since the bug was in pyproject.toml's table placement, not the workflow command.

## Findings
1. (informational, carried from attempt 1, non-blocking) `docs/tasks/T-000-foundation-skeleton.md` Handoff section again grew by one line (the "Attempt 2:" summary) — this is the pack's own template field for the implementer, not deliverable code, and its content (packages/versions, commands run, ruff/mypy/pytest results) matches what was independently reproduced here.
2. (informational, non-blocking) The attempt-2 Handoff line states `check.py` "FAILS only on `ruff format --check` for `fathom/providers.py:92`" at the time it was written. That is not reproduced at this reviewed commit: `fathom/providers.py` does not exist in this tree, and `uv run python scripts/check.py` here reports "all checks passed" with no ruff format failures. Whatever concurrent-implementer state the Handoff line described was evidently resolved (or not present) by the time of this review; no action needed since the gate is green as reviewed.

## Gate
`uv run python scripts/check.py` → all checks passed (ruff check, ruff format --check, mypy fathom, mypy fathom --platform linux, pytest --cov=fathom --cov-fail-under=80, secrets scan). `uv run pytest tests -q` → 34 passed (25 from T-000 scope + 9 from T-001's test_quotes.py, present in this wave's tree). Coverage of `fathom/`: 98.40 %.
