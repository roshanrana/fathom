# Verdict T-010 — PASS
Reviewed: e76d081b7604875d6f5e02d6de4e690010aa7c19  Tier: T2  Attempt: 1

## Acceptance criteria

- **AC1 — PASS with documented deviation (D-008).** `metrics/headline.json` matches the LLD §5
  shape (validated with a strict pydantic model in tests); the three timing fields
  (`generated_at`, `briefing_offline.latency_ms_median`, `timing_ms`) are isolated in
  `metrics/timing.json` (gitignored). `parser.coverage == 1.0`, `guard.escapes == 0`,
  `briefing_offline.verified_share == 1.0`, `injection.passed == true` — all confirmed by direct
  read of `metrics/headline.json` and by re-running the bench. Two independent `python -m
  fathom.bench` runs (via scratchpad copies, outside the repo tree) produced byte-identical
  `headline.json`. `retrieval.hit_rate` measures **0.7571** (106/140) against the pack's `>= 0.9`
  clause — the pack's own "## Blocked" section, honestly measured against the frozen
  `retrieval.py`/golden-query set (out of T-010's file scope), routed to the Planner, who
  accepted it as documented deviation D-008 for T-013. `metrics/card.json` correctly marks
  `retrieval_hit_rate` as `"status": "warn"` (target 0.9, value 0.7571) while every other KPI is
  `"pass"`/`"info"`. Judged PASS per the Planner's acceptance.
- **AC2 — PASS.** `render.py` (no args) writes `card.json`+`card.md`; `--check` exits 0 when
  current (`metrics card is current`). Independently edited `kpis[0].value` in the live
  `card.json` and reran `--check`: exited 1 with `card.json/card.md are stale vs headline.json`;
  file restored via `git checkout -- metrics/card.json` immediately after.
- **AC3 — PASS.** `scripts/check.py` STEPS: `ruff check` → `ruff format --check` → `mypy fathom`
  → `mypy fathom --platform linux` → `pytest` → `secrets scan` → `bench` → `bench drift` →
  `card drift` — the three new steps run in the specified order, immediately after the secrets
  scan (confirmed both by reading `scripts/check.py` and by the live gate run's printed order).
  Full gate: `all checks passed`.
- **AC4 — PASS.** A standalone `python -m fathom.bench` run completed in ~3.3s wall clock (well
  under 120s); `test_nfr001_run_bench_completes_within_wall_clock_budget` asserts <180s and
  `test_nfr001_offline_latency_median_under_budget` asserts <=5000ms. Bench uses only the
  default (offline) provider throughout `_briefing_offline_stats`/`_injection_check`.
- **AC5 — PASS.** `tests/test_bench.py` defines a strict pydantic model (`_Headline` +
  sub-models, `extra="forbid"`) validating the frozen shape, asserts determinism
  (`test_nfr004_two_runs_are_byte_identical_except_timing`), and checks KPI statuses
  (`test_fr015_render_marks_retrieval_hit_rate_warn_below_target`, `..._pass_at_target`,
  `..._false_positives_are_always_info`). All 16 tests follow the required
  `test_fr015_*`/`test_nfr001_*`/`test_nfr004_*`/`test_nfr007_*`/`test_nfr008_*` naming.
- **AC6 — PASS.** `uv run mypy fathom` (strict) is clean, including `bench.py`. `uv run ruff
  check fathom/bench.py tests/test_bench.py` is clean. `pyproject.toml`'s
  `[tool.ruff] extend-exclude` includes `"metrics"` (excludes `render.py` from ruff, matching the
  Lodestar precedent), and `[tool.mypy] packages = ["fathom"]` scopes mypy to the `fathom`
  package only, so `metrics/render.py` is never type-checked. `render.py` is stdlib-only (no
  third-party imports).

## Verification checklist

- [x] scope respected — diff touches exactly `docs/tasks/T-010-bench-metrics.md`,
  `fathom/bench.py`, `metrics/{render.py,headline.json,card.json,card.md}`, `scripts/check.py`
  (STEPS-only), `tests/test_bench.py`; no `.gitignore` edit was needed (`metrics/timing.json`
  was already ignored) and `pyproject.toml`/`uv.lock` are untouched.
- [x] determinism proven — two independent bench runs, diffed outside git, byte-identical.
- [x] gate steps added in the specified order — verified in source and in a live run.
- [x] tests named with RTM IDs — `test_fr015_*`, `test_nfr001_*`, `test_nfr004_*`,
  `test_nfr007_*`, `test_nfr008_*` all present.
- [x] no new dependencies — `pyproject.toml`/`uv.lock` absent from the commit diff.
- [x] timing fields isolated in `metrics/timing.json` (gitignored) — confirmed via
  `bench.py` source and `.gitignore`; `git status --short` clean after bench runs apart from
  the (ignored) `metrics/timing.json`.

## Findings

- **Non-blocking (scheduled T-013 per D-008):** `briefing_offline.latency_ms_median` reads back
  as `0` in `metrics/card.json`. `_briefing_offline_stats` derives it from the offline audit
  log's `latency_ms` field, which measures the stub/offline provider's own recorded latency, not
  wall-clock time of the full briefing generation — an implausibly small/zero value for a "P95
  latency" style KPI. Not a T-010 defect (bench faithfully reports what `briefing.py`'s audit
  records); flagged for the same T-013 follow-up as D-008.
- **Deviation D-008 (Planner-accepted):** `retrieval.hit_rate = 0.7571` vs pack target `>= 0.9`;
  honestly measured, correctly surfaced as `"warn"` in the card, root cause (`retrieval.py`
  ranking) out of T-010 scope.
- No CRITICAL or HIGH issues. Working tree left clean (`git status --short` empty) after all
  verification runs; any transient `metrics/headline.json`/`card.json` edits made during
  verification were restored with `git checkout --`.

## Gate

`uv run python scripts/check.py` → `all checks passed` in **~56s** wall clock (218 collected,
217 passed / 1 skipped, 95.47% coverage, `fathom/bench.py` at 90%). Targeted commands also
independently green: `uv run pytest tests/test_bench.py -q` (16 passed), `uv run ruff check
fathom/bench.py tests/test_bench.py` (clean), `uv run mypy fathom` (clean, strict).
